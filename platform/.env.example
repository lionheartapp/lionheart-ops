# Org / Tenant (SaaS: per-deployment or from DB)
# ORG_NAME="School Name"
# ORG_WEBSITE="https://school.edu"
# ORG_SLUG="school-slug"

# Supabase (PostgreSQL)
# Get strings from: Supabase Dashboard → Connect (or Project Settings → Database)
#
# Option A – Session pooler (port 5432) – best for Prisma from Cursor/local
# Use this for BOTH DATABASE_URL and DIRECT_URL to avoid TLS/cert issues:
#   DATABASE_URL="postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres?sslmode=require"
#   DIRECT_URL="postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres?sslmode=require"
#
# If you still see "bad certificate format", try (dev only):
#   ...postgres?sslmode=no-verify
#
# Option B – Transaction pooler for app, Session for migrations
#   DATABASE_URL="postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true"
#   DIRECT_URL="postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres?sslmode=require"
#
# Add connection_limit=5 (or 3–10) to reduce connection churn and slow-query load.
DATABASE_URL="postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres?sslmode=require&connection_limit=5"
DIRECT_URL="postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres?sslmode=require"

# Supabase anon key (for client-side auth)
NEXT_PUBLIC_SUPABASE_URL="https://[project-ref].supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key"

# OpenAI (for Event Request LLM parsing, receipt OCR, monthly report AI tips)
OPENAI_API_KEY="sk-..."

# Completion flow: receipt OCR uses GPT-4 Vision
# Monthly report: AI generates cost-saving tip from expense + MaintenanceTip data

# Public URLs (for OAuth redirects; must match Google Cloud Console)
# NEXT_PUBLIC_PLATFORM_URL="http://localhost:3001"
# NEXT_PUBLIC_LIONHEART_URL="http://localhost:5173"

# Google OAuth (create at https://console.cloud.google.com/apis/credentials)
# GOOGLE_CLIENT_ID="your-client-id.apps.googleusercontent.com"
# GOOGLE_CLIENT_SECRET="your-client-secret"
# Add redirect URI: {NEXT_PUBLIC_PLATFORM_URL}/api/auth/google/callback

# Monthly cron report (optional)
# CRON_SECRET="your-secret"           # Require ?secret=... or Bearer token
# FACILITIES_DIRECTOR_EMAIL="director@school.org"
# RESEND_API_KEY="re_..."             # From https://resend.com/api-keys
# RESEND_FROM_EMAIL="Facilities Report <reports@yourdomain.com>"  # Verify domain in Resend
