import SubscriptionSettings from './SubscriptionSettings'

/** Manage Subscription — Super Admin only. Site license pricing (Starter / Pro / Enterprise). */
export default function BillingPage() {
  return <SubscriptionSettings />
}
