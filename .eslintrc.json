{
  "extends": ["next/core-web-vitals"],
  "rules": {
    "no-restricted-syntax": [
      "warn",
      {
        "selector": "JSXOpeningElement[name.name='input']",
        "message": "Don't use raw <input>. Use <Input>, <Checkbox>, <Radio>, or <SearchInput> from @/components/ui/ instead. For file uploads, see existing FileInput patterns. If you absolutely need a raw input, add an eslint-disable comment with a reason."
      },
      {
        "selector": "JSXOpeningElement[name.name='select']",
        "message": "Don't use raw <select>. Use <Select> from @/components/ui/Select instead."
      },
      {
        "selector": "JSXOpeningElement[name.name='textarea']",
        "message": "Don't use raw <textarea>. Use <Textarea> from @/components/ui/Textarea instead."
      }
    ]
  },
  "overrides": [
    {
      "files": ["src/components/ui/**/*.{ts,tsx}"],
      "rules": {
        "no-restricted-syntax": "off"
      }
    },
    {
      "files": ["**/*.test.{ts,tsx}", "**/__tests__/**/*.{ts,tsx}", "e2e/**/*.{ts,tsx}"],
      "rules": {
        "no-restricted-syntax": "off"
      }
    },
    {
      "files": ["src/app/api/**/*.ts"],
      "rules": {
        "no-restricted-imports": [
          "warn",
          {
            "paths": [
              {
                "name": "@/lib/db",
                "importNames": ["rawPrisma"],
                "message": "Use the org-scoped `prisma` client in route handlers. rawPrisma skips tenant isolation. If rawPrisma is genuinely needed (auth lookup, etc.), add // eslint-disable-next-line no-restricted-imports with a reason."
              }
            ]
          }
        ]
      }
    }
  ]
}
