/**
 * Audit ref L3: Shared skeleton primitives + compositions.
 *
 * Use these in place of hand-rolled `animate-pulse` blocks so skeleton
 * cadence, color tone, and corner radii stay consistent across the app.
 */

export {
  Skeleton,
  SkeletonCard,
  SkeletonList,
  SkeletonTable,
  SkeletonText,
} from './Skeleton'
