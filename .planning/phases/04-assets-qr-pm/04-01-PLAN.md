---
phase: 04-assets-qr-pm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/lib/services/maintenanceAssetService.ts
  - src/lib/permissions.ts
  - src/lib/db/index.ts
  - src/app/api/maintenance/assets/route.ts
  - src/app/api/maintenance/assets/[id]/route.ts
  - src/app/api/maintenance/assets/upload-url/route.ts
  - src/app/api/maintenance/assets/[id]/qr/route.ts
  - src/components/maintenance/AssetRegisterTable.tsx
  - src/components/maintenance/AssetRegisterFilters.tsx
  - src/components/maintenance/AssetCreateDrawer.tsx
  - src/app/[tenant]/maintenance/assets/page.tsx
autonomous: true
requirements:
  - ASSET-01
  - ASSET-02
  - ASSET-03
  - QR-01

must_haves:
  truths:
    - "An admin can create a new asset with all identity, location, financial, and photo fields via slide-over drawer"
    - "Assets appear in a filterable, sortable table with columns: asset number, name, category, make/model, location, status, warranty expiry, replacement cost"
    - "Every asset gets a unique auto-generated AST-XXXX number"
    - "Every asset has a QR code generated from its URL"
  artifacts:
    - path: "src/lib/services/maintenanceAssetService.ts"
      provides: "Asset CRUD, asset number generation, cost aggregation"
      exports: ["createAsset", "getAssets", "getAssetById", "updateAsset", "deleteAsset", "generateAssetNumber"]
    - path: "src/app/api/maintenance/assets/route.ts"
      provides: "GET list + POST create endpoints"
      exports: ["GET", "POST"]
    - path: "src/app/api/maintenance/assets/[id]/route.ts"
      provides: "GET detail, PATCH update, DELETE soft-delete"
      exports: ["GET", "PATCH", "DELETE"]
    - path: "src/components/maintenance/AssetRegisterTable.tsx"
      provides: "Filterable sortable asset table"
    - path: "src/components/maintenance/AssetCreateDrawer.tsx"
      provides: "Slide-over drawer for asset creation"
  key_links:
    - from: "src/components/maintenance/AssetRegisterTable.tsx"
      to: "/api/maintenance/assets"
      via: "TanStack Query useQuery"
      pattern: "useQuery.*maintenance.*assets"
    - from: "src/components/maintenance/AssetCreateDrawer.tsx"
      to: "/api/maintenance/assets"
      via: "useMutation POST"
      pattern: "useMutation.*assets"
    - from: "src/lib/services/maintenanceAssetService.ts"
      to: "rawPrisma.maintenanceAssetCounter"
      via: "atomic upsert for AST-XXXX"
      pattern: "maintenanceAssetCounter\\.upsert"
---

<objective>
Asset register backend, CRUD API, asset list UI with filters, and QR code generation endpoint.

Purpose: Establishes the asset data layer and primary list view that all other Phase 4 plans build on. Assets are the central entity linking QR codes, PM schedules, and cost tracking.
Output: Working asset register with create/read/update/delete, filterable table, and QR code API.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-assets-qr-pm/04-CONTEXT.md
@.planning/phases/04-assets-qr-pm/04-RESEARCH.md
@prisma/schema.prisma
@src/lib/db/index.ts
@src/lib/permissions.ts
@src/lib/services/maintenanceTicketService.ts
@src/components/maintenance/WorkOrdersTable.tsx
@src/components/maintenance/WorkOrdersFilters.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema updates, asset service, and all API routes</name>
  <files>
    prisma/schema.prisma,
    src/lib/services/maintenanceAssetService.ts,
    src/lib/permissions.ts,
    src/lib/db/index.ts,
    src/app/api/maintenance/assets/route.ts,
    src/app/api/maintenance/assets/[id]/route.ts,
    src/app/api/maintenance/assets/upload-url/route.ts,
    src/app/api/maintenance/assets/[id]/qr/route.ts
  </files>
  <action>
1. **Schema updates** (`prisma/schema.prisma`):
   - Add `pmScheduledDueDate DateTime?` field to `MaintenanceTicket` model
   - Add `pmChecklistItems String[] @default([])` field to `MaintenanceTicket` model
   - Add `pmChecklistDone Boolean[] @default([])` field to `MaintenanceTicket` model
   - Add `estimatedRepairCostUSD Float?` field to `MaintenanceTicket` model (LABOR-07 prep)
   - Add `@@unique([pmScheduleId, pmScheduledDueDate])` constraint to `MaintenanceTicket` (for PM-10 idempotency)
   - Verify `MaintenanceAsset`, `PmSchedule`, `MaintenanceLaborEntry`, `MaintenanceCostEntry`, `MaintenanceAssetCounter` models already exist
   - Run `npm run db:push`

2. **Permissions** (`src/lib/permissions.ts`):
   - Add asset permissions if not present: `ASSETS_READ` ("assets:read"), `ASSETS_CREATE` ("assets:create"), `ASSETS_UPDATE` ("assets:update"), `ASSETS_DELETE` ("assets:delete")
   - Add to DEFAULT_ROLES: super-admin and maintenance-head get all 4; technician gets ASSETS_READ; member/viewer get ASSETS_READ
   - Install new npm packages: `npm install qrcode @types/qrcode jspdf html5-qrcode`

3. **Org-scope registration** (`src/lib/db/index.ts`):
   - Verify `MaintenanceAsset`, `PmSchedule`, `MaintenanceLaborEntry`, `MaintenanceCostEntry` are in the orgScopedModels set
   - Verify `MaintenanceAsset` is in the softDeleteModels set
   - Add any that are missing

4. **Asset service** (`src/lib/services/maintenanceAssetService.ts`):
   - `generateAssetNumber(orgId)`: Use rawPrisma.$transaction atomic upsert on MaintenanceAssetCounter (mirror MaintenanceCounter pattern from maintenanceTicketService.ts). Returns `AST-0001` format.
   - `createAsset(orgId, data)`: Validate with Zod, generate asset number, create via prisma
   - `getAssets(orgId, filters)`: Filterable query with: category, buildingId, areaId, roomId, status, warrantyStatus (active/expired/none), keyword search (name, assetNumber, make, model). Sortable by assetNumber (default asc), name, category, warrantyExpiry, replacementCost. Include building/area/room names in response.
   - `getAssetById(orgId, id)`: Full asset with relations (building, area, room, school)
   - `updateAsset(orgId, id, data)`: Partial update with Zod validation
   - `deleteAsset(orgId, id)`: Soft-delete via prisma.maintenanceAsset.delete
   - Asset status enum values (as constants, not Prisma enum): ACTIVE, INACTIVE, DECOMMISSIONED, PENDING_DISPOSAL

5. **API routes**:
   - `GET /api/maintenance/assets` — list with filters/sort/pagination. Permission: ASSETS_READ
   - `POST /api/maintenance/assets` — create asset. Permission: ASSETS_CREATE
   - `GET /api/maintenance/assets/[id]` — detail with relations. Permission: ASSETS_READ
   - `PATCH /api/maintenance/assets/[id]` — update. Permission: ASSETS_UPDATE
   - `DELETE /api/maintenance/assets/[id]` — soft-delete. Permission: ASSETS_DELETE
   - `POST /api/maintenance/assets/upload-url` — signed URL for asset photos (bucket: `maintenance-assets`). Permission: ASSETS_CREATE
   - `GET /api/maintenance/assets/[id]/qr` — Generate QR code SVG using `qrcode` npm. URL format: `${NEXT_PUBLIC_APP_URL}/maintenance/assets/${id}`. Return as `image/svg+xml` with `Cache-Control: public, max-age=31536000, immutable`. Permission: ASSETS_READ

All routes follow standard pattern: getOrgIdFromRequest -> getUserContext -> assertCan -> runWithOrgContext.
  </action>
  <verify>
    <automated>cd "/Users/mkerley/Desktop/Linfield Test" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - Schema pushed with PM fields on MaintenanceTicket and unique constraint
    - Asset service with full CRUD + asset number generation
    - 7 API routes all type-checking
    - QR code endpoint returns SVG
    - Asset permissions added to permissions.ts
  </done>
</task>

<task type="auto">
  <name>Task 2: Asset register list page with filters, table, and create drawer</name>
  <files>
    src/app/[tenant]/maintenance/assets/page.tsx,
    src/components/maintenance/AssetRegisterTable.tsx,
    src/components/maintenance/AssetRegisterFilters.tsx,
    src/components/maintenance/AssetCreateDrawer.tsx
  </files>
  <action>
1. **AssetRegisterFilters.tsx** (follow WorkOrdersFilters.tsx pattern):
   - Filter bar with: category dropdown (8 maintenance categories), location picker (building/area/room using useCampusLocations hook), status dropdown (Active/Inactive/Decommissioned/Pending Disposal), warranty status dropdown (Active/Expired/None), keyword search input
   - Default sort: asset number ascending
   - Sort options: Asset Number, Name, Category, Warranty Expiry, Replacement Cost
   - Glassmorphism styling: `ui-glass` card wrapping filter controls

2. **AssetRegisterTable.tsx** (follow WorkOrdersTable.tsx pattern):
   - TanStack Query `useQuery` fetching from `/api/maintenance/assets` with filter params
   - Columns: Asset Number (AST-XXXX), Name, Category, Make/Model, Location (Building > Room), Status badge, Warranty Expiry (with color: green if >90 days, amber if <90 days, red if expired), Replacement Cost ($X,XXX)
   - Row click navigates to `/maintenance/assets/[id]` detail page
   - Status badges: ACTIVE=green, INACTIVE=gray, DECOMMISSIONED=red, PENDING_DISPOSAL=amber
   - Skeleton loading with `animate-pulse` matching table layout
   - Empty state with CTA: "No assets found" + "Add your first asset" button
   - `ui-glass-table` wrapper
   - Staggered row entrance animation using listItem variant from animations.ts

3. **AssetCreateDrawer.tsx** (slide-over drawer):
   - Opens from right side (like existing drawer patterns)
   - `ui-glass-overlay` panel styling
   - Scrollable form with sections:
     - **Identity**: Name (required), Category dropdown (8 categories), Make, Model, Serial Number
     - **Location**: Building/Area/Room picker using `useCampusLocations()` hook
     - **Financials**: Purchase Date, Replacement Cost ($), Warranty Expiry date, Expected Lifespan (years), Repair Threshold % (default 50)
     - **Photos**: ImageDropZone component (reuse existing), up to 5 photos via signed URL upload to `maintenance-assets` bucket
     - **Notes**: Textarea
   - Asset number auto-assigned on creation (not shown in form, shown in success toast)
   - Zod validation on submit. useMutation POST to /api/maintenance/assets
   - On success: invalidate assets query, close drawer, show toast with AST-XXXX number
   - AnimatePresence slide-in/out animation

4. **Assets page** (`src/app/[tenant]/maintenance/assets/page.tsx`):
   - Page header: "Asset Register" title + "Add Asset" button (opens drawer) + "Scan QR" button (placeholder for Plan 02)
   - AssetRegisterFilters + AssetRegisterTable
   - Add "Assets" nav item to maintenance sidebar section (link to `/maintenance/assets`)
   - Add "PM Calendar" nav item to maintenance sidebar (link to `/maintenance/pm-calendar`, placeholder for Plan 03)
  </action>
  <verify>
    <automated>cd "/Users/mkerley/Desktop/Linfield Test" && npx tsc --noEmit 2>&1 | head -30 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - Asset register page renders at /maintenance/assets with filter bar and sortable table
    - Create drawer opens from "Add Asset" button with all form sections
    - Asset creation submits to API and asset appears in table
    - Sidebar shows Assets and PM Calendar nav items
    - Glassmorphism + animation styling matches existing patterns
  </done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- Asset CRUD API routes respond correctly (create returns AST-XXXX number)
- QR code endpoint returns valid SVG at `/api/maintenance/assets/[id]/qr`
- Asset list page renders with filters and table
- Create drawer validates and submits successfully
</verification>

<success_criteria>
- Staff can create assets via slide-over drawer with all fields
- Assets display in a filterable, sortable table with correct columns
- Each asset has an auto-generated AST-XXXX number
- QR code API generates SVG from asset URL
- Asset permissions enforce access control
</success_criteria>

<output>
After completion, create `.planning/phases/04-assets-qr-pm/04-01-SUMMARY.md`
</output>
