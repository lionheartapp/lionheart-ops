---
phase: 04-assets-qr-pm
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/app/[tenant]/maintenance/assets/[id]/page.tsx
  - src/components/maintenance/AssetDetailPage.tsx
  - src/components/maintenance/AssetRepairGauge.tsx
  - src/components/maintenance/QRCodeThumbnail.tsx
  - src/components/maintenance/QRScannerModal.tsx
  - src/components/maintenance/QRScannerInner.tsx
  - src/app/api/maintenance/assets/[id]/label/route.ts
  - src/app/api/maintenance/assets/labels/route.ts
autonomous: true
requirements:
  - ASSET-04
  - ASSET-05
  - ASSET-06
  - QR-02
  - QR-03
  - QR-04
  - QR-05

must_haves:
  truths:
    - "Asset detail page shows equipment identity, cost health gauge, ticket history, and upcoming PM"
    - "Scanning a QR code on equipment opens the asset detail page"
    - "In-app QR scanner recognizes asset QR codes and navigates to asset detail"
    - "User can print individual QR labels or batch Avery sheets as PDF"
    - "Repair threshold alert banner appears when cumulative repairs exceed configured percentage"
    - "Manual asset number entry on ticket submission works as fallback"
  artifacts:
    - path: "src/components/maintenance/AssetDetailPage.tsx"
      provides: "Full asset detail with identity, cost gauge, tickets, PM"
    - path: "src/components/maintenance/AssetRepairGauge.tsx"
      provides: "Green-amber-red repair-vs-replace progress bar + alert banner"
    - path: "src/components/maintenance/QRScannerModal.tsx"
      provides: "In-app camera QR scanner with dynamic import"
    - path: "src/app/api/maintenance/assets/[id]/label/route.ts"
      provides: "Individual label PDF generation"
  key_links:
    - from: "src/components/maintenance/AssetDetailPage.tsx"
      to: "/api/maintenance/assets/[id]"
      via: "TanStack Query useQuery"
      pattern: "useQuery.*assets.*id"
    - from: "src/components/maintenance/QRScannerModal.tsx"
      to: "/maintenance/assets/[id]"
      via: "router.push after QR decode"
      pattern: "router\\.push.*maintenance.*assets"
    - from: "src/components/maintenance/AssetRepairGauge.tsx"
      to: "asset.replacementCost and cumulative repair cost"
      via: "percentage calculation"
      pattern: "cumulativeRepairCost.*replacementCost"
---

<objective>
Asset detail page with full equipment info, repair-vs-replace cost gauge, ticket history, PM schedules, QR scanning, label printing, and manual asset search fallback.

Purpose: Completes the asset UX story — staff scan QR on equipment, see asset health, check ticket history, and report new issues. The repair gauge is the key differentiator showing when equipment should be replaced.
Output: Asset detail page, QR scanner, label printing, and ticket submission asset search.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-assets-qr-pm/04-CONTEXT.md
@.planning/phases/04-assets-qr-pm/04-RESEARCH.md
@.planning/phases/04-assets-qr-pm/04-01-SUMMARY.md
@src/lib/services/maintenanceAssetService.ts
@src/components/maintenance/SubmitRequestWizard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Asset detail page with repair gauge, ticket history, and PM section</name>
  <files>
    src/app/[tenant]/maintenance/assets/[id]/page.tsx,
    src/components/maintenance/AssetDetailPage.tsx,
    src/components/maintenance/AssetRepairGauge.tsx,
    src/components/maintenance/QRCodeThumbnail.tsx
  </files>
  <action>
1. **Extend asset service** — Add `getAssetWithDetails(orgId, id)` to `maintenanceAssetService.ts`:
   - Fetch asset with building/area/room relations
   - Fetch all MaintenanceTickets linked to this asset (include status, ticketNumber, title, createdAt, closedAt)
   - Fetch all PmSchedules linked to this asset (include name, recurrenceType, nextDueDate, status)
   - Compute cumulative repair cost: sum of all MaintenanceCostEntry.amount across ALL tickets for this asset PLUS computed labor cost (laborEntry.durationMinutes/60 * technicianProfile.loadedHourlyRate)
   - Return all in a single response object

2. **Extend GET /api/maintenance/assets/[id]** — Return the enriched detail response with ticketHistory, pmSchedules, and cumulativeRepairCost fields.

3. **AssetRepairGauge.tsx**:
   - Props: `cumulativeRepairCost: number, replacementCost: number, thresholdPct: number`
   - Progress bar: width = `(cumulativeRepairCost / replacementCost) * 100`, capped at 100%
   - Color: green if <50% of threshold, amber if 50-99% of threshold, red if >= threshold
   - Always show: "$X,XXX of $Y,YYY replacement cost" label
   - Alert banner: Only appears when `cumulativeRepairCost >= replacementCost * thresholdPct`. Text: "Cumulative repairs ($X,XXX) have reached N% of replacement cost ($Y,YYY). Consider replacement."
   - Banner uses `bg-red-50 border-red-200 text-red-800` styling with warning icon
   - Gauge uses `ui-glass` card wrapper

4. **QRCodeThumbnail.tsx**:
   - Renders `<img src="/api/maintenance/assets/${assetId}/qr" />` with lazy loading
   - Click opens larger view in a simple modal
   - Shows "Print Label" and "Print Batch" buttons below (trigger label API routes)

5. **AssetDetailPage.tsx** (full page component):
   - **Top section**: Photo carousel (or single photo), asset name, AST-XXXX number, make/model/serial, category badge, location hierarchy (Building > Area > Room), QR code thumbnail, warranty status badge (green "Active" / red "Expired" / gray "N/A")
   - **Cost & health section**: AssetRepairGauge component with cumulative vs replacement
   - **Ticket history section**: `ui-glass-table` with columns: Ticket #, Title, Status badge, Created, Closed. Link each row to ticket detail. Show open tickets first, then closed.
   - **Upcoming PM section**: List of PM schedules with name, recurrence description, next due date, assigned tech. Link to PM detail (placeholder for Plan 03).
   - **Actions**: "Report Issue" button navigates to SubmitRequestWizard with query params `?assetId={id}&buildingId={bid}&areaId={aid}&roomId={rid}` to pre-fill
   - **Edit**: "Edit Asset" button opens AssetCreateDrawer in edit mode (pre-filled with current values, PATCH on save)
   - Staggered section entrance animations using fadeInUp from animations.ts
   - `ui-glass` section cards

6. **Asset detail route** (`src/app/[tenant]/maintenance/assets/[id]/page.tsx`):
   - Server component wrapper, passes asset ID to AssetDetailPage client component
  </action>
  <verify>
    <automated>cd "/Users/mkerley/Desktop/Linfield Test" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - Asset detail page renders at /maintenance/assets/[id] with all sections
    - Repair gauge shows correct percentage and alert when threshold exceeded
    - QR code thumbnail displays from API
    - Ticket history and PM sections populated from API data
    - "Report Issue" navigates to ticket submission with pre-filled asset/location
  </done>
</task>

<task type="auto">
  <name>Task 2: QR scanner, label printing, and manual asset search on ticket wizard</name>
  <files>
    src/components/maintenance/QRScannerModal.tsx,
    src/components/maintenance/QRScannerInner.tsx,
    src/app/api/maintenance/assets/[id]/label/route.ts,
    src/app/api/maintenance/assets/labels/route.ts,
    src/components/maintenance/SubmitRequestWizard/StepAsset.tsx
  </files>
  <action>
1. **QRScannerInner.tsx** (client-only, no SSR):
   - Uses `html5-qrcode` library with `Html5QrcodeScanner`
   - Config: `{ fps: 10, qrbox: { width: 250, height: 250 } }`
   - On successful scan: extract asset ID from URL pattern `/maintenance/assets/([^/?]+)`, navigate via `router.push`
   - If URL doesn't match asset pattern, show "Not a valid asset QR code" error
   - Cleanup scanner on unmount via `scanner.clear()`

2. **QRScannerModal.tsx** (SSR-safe wrapper):
   - Uses `dynamic(() => import('./QRScannerInner'), { ssr: false })` — CRITICAL to avoid window/navigator crash
   - Modal overlay with `ui-glass-overlay` styling
   - "Scan QR Code" header, camera viewfinder area, close button
   - AnimatePresence scale+fade entrance
   - Wire "Scan QR" button on assets page header to open this modal

3. **Label PDF routes**:
   - `GET /api/maintenance/assets/[id]/label` — Returns JSON with asset data needed for client-side jsPDF label generation (assetNumber, name, qrDataUrl as base64 PNG). Use `qrcode.toDataURL()` to generate base64 PNG.
   - `POST /api/maintenance/assets/labels` — Accepts `{ assetIds: string[] }`. Returns JSON with array of asset label data for batch generation. Client uses jsPDF to render Avery 5160 layout (3 columns x 10 rows per page, 1" x 2.625" labels).
   - Client-side label generation helper function in a utility file `src/lib/label-utils.ts`:
     - `generateSingleLabel(asset, qrDataUrl)` — jsPDF 2x1 inch: QR (64x64pt) left, asset number + name right
     - `generateBatchLabels(assets)` — jsPDF letter-size Avery 5160: 30 labels per page

4. **StepAsset.tsx** (new optional step in SubmitRequestWizard):
   - Search input with autocomplete: queries `/api/maintenance/assets?keyword=` as user types (debounced 300ms)
   - Shows matching assets in dropdown: AST-XXXX — Asset Name — Building > Room
   - On selection: sets assetId in wizard state, auto-fills location fields (buildingId, areaId, roomId) from asset
   - "Skip" button to proceed without asset (not all tickets are asset-related)
   - Also read query params `?assetId=` on mount — if present, auto-select that asset (for "Report Issue" from asset detail)
   - Wire into SubmitRequestWizard step flow: insert after Location step as optional step (Location > Asset > Photos > Details > Review)
   - Manual asset number entry: secondary input below search ("Or enter asset number: AST-") that does exact match lookup

5. **Update ticket submission** — When asset is selected in wizard, include `assetId` in the POST body to `/api/maintenance/tickets`. Verify the maintenanceTicketService accepts and stores assetId.
  </action>
  <verify>
    <automated>cd "/Users/mkerley/Desktop/Linfield Test" && npx tsc --noEmit 2>&1 | head -30 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - QR scanner opens camera, reads asset QR code, navigates to asset detail
    - Single label and batch label PDFs generate correctly with QR + asset info
    - Ticket submission wizard has optional asset search step with autocomplete
    - Manual AST-XXXX entry works as fallback
    - Pre-fill from asset detail "Report Issue" button works via query params
  </done>
</task>

</tasks>

<verification>
- Asset detail page loads with all sections populated
- QR scanner modal opens camera and navigates on successful scan
- Label PDF downloads work (single and batch)
- Repair gauge shows correct color and alert at threshold
- Ticket wizard asset step auto-fills location from selected asset
- `npm run build` completes
</verification>

<success_criteria>
- Staff scan QR on equipment and land on asset detail with full context
- Repair threshold alert visible when cumulative costs exceed configured %
- Labels printable for physical asset tagging
- Ticket submission can reference an asset via search, QR scan, or manual entry
</success_criteria>

<output>
After completion, create `.planning/phases/04-assets-qr-pm/04-02-SUMMARY.md`
</output>
