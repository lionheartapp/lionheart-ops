---
phase: 26-core-messaging-ui
plan: 02
type: execute
wave: 2
depends_on: [26-01]
files_modified:
  - src/components/messaging/MessageList.tsx
  - src/components/messaging/MessageBubble.tsx
  - src/components/messaging/MessageArea.tsx
  - src/components/messaging/MessagingShell.tsx
  - src/lib/hooks/useMessages.ts
autonomous: true
requirements:
  - UI-02

must_haves:
  truths:
    - "Messages display in chronological order with newest at the bottom"
    - "Scrolling up loads older messages without janking the scroll position"
    - "New realtime messages from other users appear at the bottom without a refresh"
    - "500+ messages do not degrade scroll performance"
    - "Skeleton loading shows while messages are initially fetching"
  artifacts:
    - path: "src/components/messaging/MessageList.tsx"
      provides: "Virtual-scrolled message list with reverse scroll"
    - path: "src/components/messaging/MessageBubble.tsx"
      provides: "Individual message rendering"
    - path: "src/lib/hooks/useMessages.ts"
      provides: "TanStack Query hook with cursor-based pagination"
  key_links:
    - from: "src/components/messaging/MessageList.tsx"
      to: "/api/messaging/channels/[id]/messages"
      via: "useMessages hook with cursor pagination"
      pattern: "useMessages"
    - from: "src/components/messaging/MessageList.tsx"
      to: "useRealtimeChannel"
      via: "incomingMessage appended to list"
      pattern: "incomingMessage"
---

<objective>
Virtual-scrolled message list with realtime message delivery.

Purpose: Renders the message area with react-virtuoso for virtual scrolling, cursor-based pagination for history loading, and realtime broadcast integration for live messages. This is the core reading experience.

Output: Working message list that handles 500+ messages efficiently, loads history on scroll-up, and shows new messages in real time.
</objective>

<execution_context>
@/Users/mkerley/Desktop/Linfield Test/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mkerley/Desktop/Linfield Test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-core-messaging-ui/26-CONTEXT.md
@.planning/phases/26-core-messaging-ui/26-01-SUMMARY.md

<interfaces>
From src/lib/services/messageService.ts:
```typescript
export interface MessageWithAuthor {
  id: string
  channelId: string
  authorId: string
  authorName: string
  authorAvatar: string | null
  content: string
  parentId: string | null
  replyCount: number
  editedAt: string | null
  pinnedAt: string | null
  createdAt: string
}

// GET /api/messaging/channels/[id]/messages?before=msgId&limit=50
// Returns: { ok: true, data: { messages: MessageWithAuthor[], hasMore: boolean, cursor: string | null } }
```

From src/lib/hooks/useRealtimeChannel.ts:
```typescript
export interface BroadcastMessage {
  id: string
  channelId: string
  authorId: string
  content: string
  createdAt: string
  organizationId: string
  [key: string]: unknown
}

export function useRealtimeChannel(options: {
  channelId: string | null
  orgId: string
  currentUserId: string
  currentDisplayName: string
}): {
  incomingMessage: BroadcastMessage | null
  typingUsers: TypingUser[]
  presenceState: Map<string, 'online' | 'away' | 'offline'>
  isConnected: boolean
  error: string | null
  sendTypingIndicator: () => void
}
```

From Plan 01 (MessagingShell):
```typescript
// MessagingShell manages activeChannelId state and passes it down.
// Plan 02 replaces the message area placeholder with MessageArea component.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useMessages hook and MessageBubble component</name>
  <files>
    src/lib/hooks/useMessages.ts,
    src/components/messaging/MessageBubble.tsx
  </files>
  <read_first>
    src/lib/services/messageService.ts (lines 1-60 for types),
    src/app/api/messaging/channels/[id]/messages/route.ts (first 40 lines for response shape),
    src/lib/hooks/useAuth.ts
  </read_first>
  <action>
    1. Install react-virtuoso: `npm install react-virtuoso`

    2. Create `src/lib/hooks/useMessages.ts`:
       - TanStack Query hook using `useInfiniteQuery` for cursor-based pagination (per D-06)
       - Query key: ['messaging', 'messages', channelId]
       - Fetches GET /api/messaging/channels/{channelId}/messages?before={cursor}&limit=50
       - Uses credentials: 'include' for auth
       - getNextPageParam returns the cursor from the last page when hasMore is true
       - Flattens pages into a single messages array (oldest first for display)
       - Exports: `useMessages(channelId: string | null)` returning `{ messages, fetchNextPage, hasNextPage, isFetchingNextPage, isLoading, error }`
       - Also exports a `useMarkChannelRead(channelId)` mutation that POSTs to mark-read when channel is opened (updates ChannelMember.lastReadAt). This resets unread count. Invalidates ['messaging', 'channels'] query key on success.

    3. Create `src/components/messaging/MessageBubble.tsx` (per D-07):
       - Props: message (MessageWithAuthor), isOwn (boolean), showAvatar (boolean)
       - Renders: avatar (32x32 rounded-full, only if showAvatar), author name (text-xs font-medium text-slate-600), timestamp (text-xs text-slate-400, relative using Intl.RelativeTimeFormat or a simple ago function), message content (text-sm text-slate-800)
       - Own messages: subtle bg-primary-50 rounded-xl px-3 py-2 — NOT right-aligned (Slack doesn't do that, it's confusing in group chat). Instead, own messages have a subtle tinted background.
       - Other messages: bg-white rounded-xl px-3 py-2
       - Consecutive messages from same author within 5 minutes: collapse avatar and name (showAvatar=false), only show content with reduced top margin
       - "edited" indicator: when editedAt is not null, show "(edited)" in text-xs text-slate-400 after timestamp
       - Reply count: when replyCount > 0, show "{N} replies" link text in text-xs text-primary-500 cursor-pointer (clicking handled by parent, emits onThreadClick)
       - Content rendering: plain text only in this phase. Use whitespace-pre-wrap to preserve line breaks. NO dangerouslySetInnerHTML — markdown rendering deferred to Phase 27.
  </action>
  <verify>
    <automated>cd /Users/mkerley/Desktop/Linfield Test && npx tsc --noEmit --pretty 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "useInfiniteQuery" src/lib/hooks/useMessages.ts
    - grep -q "fetchNextPage" src/lib/hooks/useMessages.ts
    - grep -q "MessageBubble" src/components/messaging/MessageBubble.tsx
    - grep -q "whitespace-pre-wrap" src/components/messaging/MessageBubble.tsx
    - grep -q "edited" src/components/messaging/MessageBubble.tsx
    - grep -rq "react-virtuoso" package.json
  </acceptance_criteria>
  <done>
    useMessages hook fetches paginated messages via TanStack Query infinite query. MessageBubble renders individual messages with avatar, name, timestamp, edited indicator, reply count link, and own-message tinting. react-virtuoso installed.
  </done>
</task>

<task type="auto">
  <name>Task 2: MessageList with virtual scroll and MessageArea with realtime integration</name>
  <files>
    src/components/messaging/MessageList.tsx,
    src/components/messaging/MessageArea.tsx,
    src/components/messaging/MessagingShell.tsx
  </files>
  <read_first>
    src/components/messaging/MessagingShell.tsx,
    src/lib/hooks/useRealtimeChannel.ts,
    src/lib/hooks/useAuth.ts
  </read_first>
  <action>
    1. Create `src/components/messaging/MessageList.tsx` (per D-06):
       - Uses react-virtuoso's Virtuoso component with `firstItemIndex` and `initialTopMostItemIndex` for reverse-chronological display (newest at bottom)
       - Props: messages (MessageWithAuthor[]), isLoading (boolean), hasMore (boolean), isFetchingMore (boolean), onLoadMore (() => void), onThreadClick ((messageId: string) => void)
       - Virtuoso config:
         - `followOutput: 'smooth'` — auto-scrolls to new messages at the bottom
         - `startReached` callback triggers onLoadMore when scrolling up (prepend pattern)
         - `firstItemIndex` = START_INDEX - messages.length (where START_INDEX = 100000, a large number for prepend support)
         - `initialTopMostItemIndex` = messages.length - 1 (start at bottom)
         - `overscan` = 200 for smooth scroll experience
       - Each item renders MessageBubble, computing isOwn and showAvatar (collapse consecutive same-author messages within 5 min)
       - Loading states: top spinner when isFetchingMore (prepending history), skeleton placeholders when isLoading (initial load — 6 skeleton message shapes with animate-pulse)
       - Empty state: when messages is empty and not loading, show "No messages yet. Start the conversation!" with a chat bubble illustration

    2. Create `src/components/messaging/MessageArea.tsx`:
       - Props: channelId (string), onThreadClick ((messageId: string) => void)
       - Calls useMessages(channelId) for paginated message data
       - Calls useRealtimeChannel({ channelId, orgId, currentUserId, currentDisplayName }) for live messages
       - Maintains a local `realtimeMessages` state array. When incomingMessage changes (from useRealtimeChannel), append it to the local array (converting BroadcastMessage to MessageWithAuthor shape by mapping fields and using 'Unknown' for missing authorName — the next API refetch will fill it in)
       - Combined messages: [...apiMessages, ...realtimeMessages] — deduplicated by id (realtime may arrive before refetch returns the same message)
       - On channelId change: clear realtimeMessages, refetch messages
       - Calls useMarkChannelRead(channelId) on mount and when channelId changes to reset unread count
       - Shows typing indicator below the message list: when typingUsers.length > 0, show "{name} is typing..." or "{name1} and {name2} are typing..." in text-xs text-slate-500 italic with a subtle pulse animation
       - Renders MessageList with all the wired props

    3. Update `src/components/messaging/MessagingShell.tsx`:
       - Replace the message area placeholder with MessageArea component when activeChannelId is set
       - Pass activeChannelId and onThreadClick (no-op for now, Plan 04 wires it) to MessageArea
       - Keep the "Select a channel" empty state when no channel is selected
  </action>
  <verify>
    <automated>cd /Users/mkerley/Desktop/Linfield Test && npx tsc --noEmit --pretty 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Virtuoso" src/components/messaging/MessageList.tsx
    - grep -q "firstItemIndex" src/components/messaging/MessageList.tsx
    - grep -q "followOutput" src/components/messaging/MessageList.tsx
    - grep -q "useRealtimeChannel" src/components/messaging/MessageArea.tsx
    - grep -q "incomingMessage" src/components/messaging/MessageArea.tsx
    - grep -q "typingUsers" src/components/messaging/MessageArea.tsx
    - grep -q "MessageArea" src/components/messaging/MessagingShell.tsx
  </acceptance_criteria>
  <done>
    Message list renders with react-virtuoso virtual scrolling. Scrolling up prepends older messages via cursor pagination. New realtime messages appear at the bottom. Typing indicators show below the message list. 500+ messages render without scroll performance degradation.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> /api/messaging/channels/[id]/messages | Message content fetched via authenticated API |
| Realtime broadcast -> UI | Messages arrive via Supabase Realtime broadcast |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04 | Information Disclosure | Message content | mitigate | API endpoint enforces channel membership check + org-scoped Prisma; only messages from accessible channels returned |
| T-26-05 | Tampering | Realtime messages | mitigate | Broadcast topic includes orgId (msg:{orgId}:{channelId}); RLS prevents cross-org subscription; UI deduplicates against API data |
| T-26-06 | Spoofing | Message author | accept | Author info comes from server-side Prisma include; broadcast payload includes authorId from trigger (DB-level, not client-controllable) |
| T-26-07 | Cross-Site Scripting | Message content | mitigate | Content rendered as plain text with whitespace-pre-wrap; no dangerouslySetInnerHTML; React auto-escapes JSX string content |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes
2. Select a channel and see messages load at the bottom
3. Scroll up to load older messages — scroll position stays stable
4. Send a message from another session — it appears in real time
5. Typing indicator shows when another user types
</verification>

<success_criteria>
- Virtual-scrolled message list renders with react-virtuoso
- Cursor-based pagination loads history on scroll-up without position jank
- Realtime messages from other users appear at the bottom
- Typing indicator displays "{name} is typing..."
- Skeleton loading during initial message fetch
</success_criteria>

<output>
After completion, create `.planning/phases/26-core-messaging-ui/26-02-SUMMARY.md`
</output>
