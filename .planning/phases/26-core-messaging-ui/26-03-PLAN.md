---
phase: 26-core-messaging-ui
plan: 03
type: execute
wave: 2
depends_on: [26-01]
files_modified:
  - src/components/messaging/Composer.tsx
  - src/components/messaging/EmojiPicker.tsx
  - src/components/messaging/MentionAutocomplete.tsx
  - src/lib/hooks/useSendMessage.ts
  - src/lib/hooks/useOrgMembers.ts
autonomous: true
requirements:
  - UI-03

must_haves:
  truths:
    - "User can type a message and send it with Enter"
    - "User can insert a newline with Shift+Enter"
    - "User can pick an emoji from a popup picker"
    - "User can type @ and see an autocomplete dropdown of org members"
    - "Sent messages appear immediately via optimistic update"
    - "File upload button is present but shows 'Coming soon' tooltip"
  artifacts:
    - path: "src/components/messaging/Composer.tsx"
      provides: "Message input with toolbar"
    - path: "src/components/messaging/EmojiPicker.tsx"
      provides: "Emoji picker popup"
    - path: "src/components/messaging/MentionAutocomplete.tsx"
      provides: "@mention autocomplete dropdown"
    - path: "src/lib/hooks/useSendMessage.ts"
      provides: "TanStack mutation for sending messages"
  key_links:
    - from: "src/components/messaging/Composer.tsx"
      to: "/api/messaging/channels/[id]/messages"
      via: "useSendMessage mutation"
      pattern: "useSendMessage"
    - from: "src/components/messaging/MentionAutocomplete.tsx"
      to: "/api/settings/users"
      via: "useOrgMembers query"
      pattern: "useOrgMembers"
---

<objective>
Message composer with emoji picker, @mention autocomplete, and send functionality.

Purpose: Builds the message input experience — the primary write path for messaging. Includes emoji selection, @mention autocomplete for tagging users, and optimistic send with Enter key.

Output: Working composer component that sends messages and integrates with the realtime typing indicator.
</objective>

<execution_context>
@/Users/mkerley/Desktop/Linfield Test/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mkerley/Desktop/Linfield Test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-core-messaging-ui/26-CONTEXT.md
@.planning/phases/26-core-messaging-ui/26-01-SUMMARY.md

<interfaces>
From src/lib/services/messageService.ts:
```typescript
export const SendMessageSchema = z.object({
  content: z.string().min(1).max(4000).trim(),
  parentId: z.string().optional(),
})

// POST /api/messaging/channels/[id]/messages
// Body: { content: string, parentId?: string }
// Returns: { ok: true, data: MessageWithAuthor }
```

From src/lib/hooks/useRealtimeChannel.ts:
```typescript
// sendTypingIndicator() — debounced to once every 3 seconds
sendTypingIndicator: () => void
```

From src/app/api/settings/users/route.ts:
```typescript
// GET /api/settings/users returns list of org members
// Each user has: id, firstName, lastName, email, avatar, roleId, status
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Send message hook, org members hook, and emoji picker</name>
  <files>
    src/lib/hooks/useSendMessage.ts,
    src/lib/hooks/useOrgMembers.ts,
    src/components/messaging/EmojiPicker.tsx
  </files>
  <read_first>
    src/app/api/messaging/channels/[id]/messages/route.ts (first 30 lines for POST shape),
    src/app/api/settings/users/route.ts (first 30 lines for GET shape),
    src/lib/hooks/useMessages.ts
  </read_first>
  <action>
    1. Create `src/lib/hooks/useSendMessage.ts`:
       - TanStack Query mutation using `useMutation`
       - Posts to `/api/messaging/channels/{channelId}/messages` with { content, parentId? }
       - Uses credentials: 'include'
       - On success: invalidates ['messaging', 'messages', channelId] and ['messaging', 'channels'] query keys
       - Exports `useSendMessage(channelId: string)` returning `{ sendMessage, isPending }`
       - The sendMessage function accepts `{ content: string, parentId?: string }`

    2. Create `src/lib/hooks/useOrgMembers.ts`:
       - TanStack Query hook that fetches GET /api/settings/users
       - Query key: ['org-members']
       - staleTime: 5 * 60 * 1000 (members don't change often)
       - Returns array of `{ id: string, firstName: string | null, lastName: string | null, email: string, avatar: string | null }`
       - Exports `useOrgMembers()` and a `useFilteredMembers(query: string)` that filters by first/last name match

    3. Install emoji-mart: `npm install @emoji-mart/react @emoji-mart/data`

    4. Create `src/components/messaging/EmojiPicker.tsx` (per D-09):
       - 'use client' component
       - Uses @emoji-mart/react Picker component
       - Props: onSelect((emoji: string) => void), onClose(() => void)
       - Renders in a floating panel positioned above the trigger button (absolute positioning, bottom-full right-0)
       - Panel uses ui-glass-dropdown class for glassmorphism style
       - Wraps in a click-outside handler to close
       - Sets theme='light', set='native', maxFrequentRows=2, previewPosition='none' for compact display
       - Dynamic import of emoji-mart data to reduce bundle size: `const data = await import('@emoji-mart/data')`
  </action>
  <verify>
    <automated>cd /Users/mkerley/Desktop/Linfield Test && npx tsc --noEmit --pretty 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "useMutation" src/lib/hooks/useSendMessage.ts
    - grep -q "useOrgMembers" src/lib/hooks/useOrgMembers.ts
    - grep -q "useFilteredMembers" src/lib/hooks/useOrgMembers.ts
    - grep -q "emoji-mart" src/components/messaging/EmojiPicker.tsx
    - grep -rq "@emoji-mart" package.json
  </acceptance_criteria>
  <done>
    Send message mutation hook created. Org members hook with filtering exported. Emoji picker component renders @emoji-mart/react with glassmorphism styling.
  </done>
</task>

<task type="auto">
  <name>Task 2: Composer component with @mention autocomplete</name>
  <files>
    src/components/messaging/Composer.tsx,
    src/components/messaging/MentionAutocomplete.tsx
  </files>
  <read_first>
    src/components/ui/form-tokens.ts,
    src/components/ui/Textarea.tsx,
    src/lib/hooks/useSendMessage.ts,
    src/lib/hooks/useOrgMembers.ts,
    src/components/messaging/EmojiPicker.tsx
  </read_first>
  <action>
    1. Create `src/components/messaging/MentionAutocomplete.tsx` (per D-09):
       - Props: query (string — text after the @), position ({ top: number, left: number } — cursor position), onSelect((user: { id: string, name: string }) => void), onClose(() => void)
       - Uses useFilteredMembers(query) to get matching members, debounced 200ms
       - Renders a dropdown list at the given position (absolute, z-50)
       - Each item shows: avatar (24x24 rounded-full), full name, and email in text-xs text-slate-400
       - Keyboard navigation: up/down arrow to select, Enter to confirm, Escape to close
       - Max 6 results shown
       - Also shows @channel, @here, @team as special entries when query is empty or matches
       - Panel uses ui-glass-dropdown class

    2. Create `src/components/messaging/Composer.tsx` (per D-09, D-10, D-11):
       - Props: channelId (string), onSendTyping (() => void — connected to sendTypingIndicator from useRealtimeChannel), parentId? (string — for thread replies, used in Plan 04)
       - Layout: a container at the bottom of the message area with:
         - Toolbar row: emoji button (Smile icon), file upload button (Paperclip icon with "Coming soon" title tooltip per D-10), formatting hint text "Markdown supported"
         - Textarea: uses a plain textarea element (NOT the UI Textarea component — the composer needs special keyboard handling). Styled with form-tokens INPUT_CLASSES. Auto-resizes up to 6 lines max (min-h-[44px] max-h-[160px] overflow-y-auto resize-none).
         - Send button row: Send button (SendHorizonal icon from lucide-react) positioned at bottom-right of the textarea, bg-primary-500 text-white rounded-full w-8 h-8 flex items-center justify-center. Disabled when content is empty or isPending.
       - Keyboard handling (per D-11):
         - Enter (no shift): submit message (call sendMessage({ content, parentId })), clear textarea, focus textarea
         - Shift+Enter: insert newline (default behavior)
         - On every keystroke: call onSendTyping() (the hook debounces internally)
       - @mention detection:
         - On input change, detect if cursor is after an @ character (regex: /@(\w*)$/)
         - When detected: calculate cursor position relative to textarea, show MentionAutocomplete
         - On mention select: replace the @query text with `@{userName}` and close autocomplete
       - Emoji picker:
         - Clicking emoji button toggles EmojiPicker visibility
         - On emoji select: insert emoji character at cursor position in textarea, close picker
       - Optimistic send: after sending, immediately append the message to the local list (via a callback prop or by invalidating queries). The useSendMessage hook handles query invalidation.
       - Container styling: bg-white border-t border-slate-200 px-4 py-3
  </action>
  <verify>
    <automated>cd /Users/mkerley/Desktop/Linfield Test && npx tsc --noEmit --pretty 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "onSendTyping" src/components/messaging/Composer.tsx
    - grep -q "Shift.*Enter\|shiftKey" src/components/messaging/Composer.tsx
    - grep -q "useSendMessage" src/components/messaging/Composer.tsx
    - grep -q "MentionAutocomplete" src/components/messaging/Composer.tsx
    - grep -q "EmojiPicker" src/components/messaging/Composer.tsx
    - grep -q "Paperclip\|paperclip\|file.*upload" src/components/messaging/Composer.tsx
    - grep -q "useFilteredMembers" src/components/messaging/MentionAutocomplete.tsx
    - grep -q "@channel\|@here" src/components/messaging/MentionAutocomplete.tsx
  </acceptance_criteria>
  <done>
    Composer renders at bottom of message area with textarea, emoji picker, @mention autocomplete, file upload button (disabled), and send on Enter. Typing indicator fires on keystrokes. Messages send via API and invalidate queries.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Composer -> /api/messaging/channels/[id]/messages | Message content posted via authenticated API |
| @mention query -> /api/settings/users | Org member list fetched for autocomplete |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-08 | Cross-Site Scripting | Message content injection | mitigate | Content sent as plain text to API; server-side Zod validates max 4000 chars; rendering uses React JSX (auto-escaped), no dangerouslySetInnerHTML |
| T-26-09 | Information Disclosure | Org members list | accept | /api/settings/users already requires auth + org-scope; all org members can see each other's names (expected behavior for @mentions) |
| T-26-10 | Denial of Service | Rapid message sending | mitigate | Send button disabled while isPending; API has rate limiting at middleware level |
| T-26-11 | Tampering | Message content | mitigate | Content validated by Zod schema server-side (1-4000 chars, trimmed); parentId validated as valid message ID |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes
2. Type a message and press Enter — message sends and appears in the list
3. Press Shift+Enter — newline inserted, message NOT sent
4. Click emoji button — picker appears, select emoji — inserted at cursor
5. Type @ — autocomplete shows org members, select one — @name inserted
6. File upload button shows "Coming soon" tooltip
</verification>

<success_criteria>
- Composer sends messages on Enter, newlines on Shift+Enter
- Emoji picker opens and inserts selected emoji
- @mention autocomplete shows filtered org members with keyboard navigation
- File upload button present but disabled with tooltip
- Typing indicator fires while typing (verified in Plan 02's typing display)
</success_criteria>

<output>
After completion, create `.planning/phases/26-core-messaging-ui/26-03-SUMMARY.md`
</output>
