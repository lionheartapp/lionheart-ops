---
phase: 26-core-messaging-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sidebar/MainNavContent.tsx
  - src/components/sidebar/Sidebar.tsx
  - src/app/messaging/page.tsx
  - src/app/messaging/layout.tsx
  - src/components/messaging/ChannelList.tsx
  - src/components/messaging/ChannelListItem.tsx
  - src/components/messaging/MessagingShell.tsx
  - src/lib/hooks/useChannels.ts
  - src/lib/hooks/useMessagingUnread.ts
autonomous: true
requirements:
  - UI-01
  - UI-05

must_haves:
  truths:
    - "Sidebar shows Messaging nav item with unread badge when messagingEnabled is true"
    - "Sidebar hides Messaging nav item when messagingEnabled is false"
    - "Messaging page renders with ~280px channel list on the left and message area on the right"
    - "Channel list shows all channels the user belongs to, grouped by type"
    - "Clicking a channel updates the active channel without page navigation"
  artifacts:
    - path: "src/app/messaging/page.tsx"
      provides: "Messaging page entry point"
    - path: "src/components/messaging/MessagingShell.tsx"
      provides: "Two-column layout shell"
    - path: "src/components/messaging/ChannelList.tsx"
      provides: "Channel list sidebar component"
    - path: "src/lib/hooks/useChannels.ts"
      provides: "TanStack Query hook for channel fetching"
    - path: "src/lib/hooks/useMessagingUnread.ts"
      provides: "Total unread count for sidebar badge"
  key_links:
    - from: "src/components/sidebar/MainNavContent.tsx"
      to: "/messaging"
      via: "PrefetchLink with unread badge"
      pattern: "useModuleEnabled.*messaging"
    - from: "src/components/messaging/ChannelList.tsx"
      to: "/api/messaging/channels"
      via: "useChannels hook with TanStack Query"
      pattern: "useChannels"
---

<objective>
Messaging page shell with sidebar nav item and channel list.

Purpose: Establishes the messaging page route, two-column layout, channel list sidebar, and sidebar navigation entry point with unread badge. This is the foundation all other messaging UI plans build on.

Output: Working messaging page with channel list, sidebar nav item gated by messagingEnabled, and active channel selection state.
</objective>

<execution_context>
@/Users/mkerley/Desktop/Linfield Test/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mkerley/Desktop/Linfield Test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-core-messaging-ui/26-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/lib/services/channelService.ts:
```typescript
// getChannels returns array of shaped channels:
interface ShapedChannel {
  id: string
  name: string
  slug: string
  type: string // 'PUBLIC' | 'PRIVATE' | 'DM' | 'GROUP_DM'
  description: string | null
  topic: string | null
  archivedAt: string | null
  createdAt: string
  updatedAt: string
  createdById: string
  memberCount: number
  members?: ShapedMember[]
  lastMessage: { id: string; content: string; authorId: string; createdAt: string } | null
}
```

From src/lib/hooks/useModuleEnabled.ts:
```typescript
export function useModuleEnabled(moduleId: string): { enabled: boolean; loading: boolean }
```

From src/components/messaging/RealtimeProvider.tsx:
```typescript
export function RealtimeProvider({ children }: { children: ReactNode }): JSX.Element
export function useRealtime(): { supabaseClient: SupabaseClient | null; isConnected: boolean; connectionError: string | null }
```

From src/components/sidebar/MainNavContent.tsx:
```typescript
// Nav items use PrefetchLink with this active pattern:
const isActive = (path: string) => pathname === path || pathname.startsWith(path + '/')
// Athletics module gating pattern: athleticsEnabled && canWriteAthletics ? <li>...</li> : null
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sidebar nav item with unread badge and messaging hooks</name>
  <files>
    src/components/sidebar/MainNavContent.tsx,
    src/components/sidebar/Sidebar.tsx,
    src/lib/hooks/useChannels.ts,
    src/lib/hooks/useMessagingUnread.ts
  </files>
  <read_first>
    src/components/sidebar/MainNavContent.tsx,
    src/components/sidebar/Sidebar.tsx,
    src/lib/hooks/useModuleEnabled.ts,
    src/lib/services/channelService.ts (lines 1-145 for types)
  </read_first>
  <action>
    1. Create `src/lib/hooks/useChannels.ts` — TanStack Query hook that fetches GET /api/messaging/channels. Query key: ['messaging', 'channels']. Returns typed array of channels. Include a `useChannel(channelId)` query for single-channel fetch.

    2. Create `src/lib/hooks/useMessagingUnread.ts` — derives total unread count from useChannels data. Iterates channel list, sums up member.unreadCount for the current user's membership. Exports `useMessagingUnread(): number`. This avoids a separate API call — the channel list already includes unread counts.

    3. Add Messaging nav item to MainNavContent.tsx (per D-14):
       - Import `MessageSquare` from lucide-react
       - Add `messagingEnabled: boolean` and `messagingModuleLoading: boolean` props to MainNavContentProps interface
       - Add nav item between Events and Athletics sections, following the exact same pattern as the Athletics conditional render:
         - When `messagingModuleLoading` → show skeleton pulse placeholder
         - When `messagingEnabled` → show PrefetchLink to `/messaging` with MessageSquare icon and "Messaging" label
         - When disabled → render null
       - Add unread badge: use `useMessagingUnread()` hook. When count > 0, show a badge (same style as NotificationBellIcon badge: absolute positioned, bg-red-500 text-white text-xs rounded-full min-w-[18px] h-[18px]). When count > 9, show "9+".

    4. Update Sidebar.tsx to pass `messagingEnabled` and `messagingModuleLoading` props down to MainNavContent. Use `useModuleEnabled('messaging')` — same pattern as athletics.
  </action>
  <verify>
    <automated>cd /Users/mkerley/Desktop/Linfield Test && npx tsc --noEmit --pretty 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "MessageSquare" src/components/sidebar/MainNavContent.tsx
    - grep -q "messagingEnabled" src/components/sidebar/MainNavContent.tsx
    - grep -q "useMessagingUnread" src/lib/hooks/useMessagingUnread.ts
    - grep -q "useChannels" src/lib/hooks/useChannels.ts
    - grep -q "messaging.*channels" src/lib/hooks/useChannels.ts
  </acceptance_criteria>
  <done>
    Sidebar shows "Messaging" nav item with MessageSquare icon, gated by messagingEnabled module check. Unread badge displays when count > 0. Two new hooks export useChannels and useMessagingUnread.
  </done>
</task>

<task type="auto">
  <name>Task 2: Messaging page, layout, shell, and channel list components</name>
  <files>
    src/app/messaging/page.tsx,
    src/app/messaging/layout.tsx,
    src/components/messaging/MessagingShell.tsx,
    src/components/messaging/ChannelList.tsx,
    src/components/messaging/ChannelListItem.tsx
  </files>
  <read_first>
    src/app/dashboard/page.tsx (lines 1-30 for page pattern),
    src/app/events/page.tsx (lines 1-30 for page pattern),
    src/components/messaging/RealtimeProvider.tsx,
    src/lib/hooks/useAuth.ts (for user context)
  </read_first>
  <action>
    1. Create `src/app/messaging/layout.tsx` — wraps children in RealtimeProvider from Phase 25 (per D-03). Uses 'use client'. Imports RealtimeProvider from '@/components/messaging/RealtimeProvider'.

    2. Create `src/app/messaging/page.tsx` (per D-02) — renders MessagingShell. Uses 'use client'. Minimal page component that just renders the shell.

    3. Create `src/components/messaging/MessagingShell.tsx` (per D-01):
       - Two-column flex layout: channel list sidebar (~280px, flex-shrink-0) on the left, message area (flex-1) on the right
       - Uses ui-glass classes for the container background per glassmorphism design system
       - Manages `activeChannelId` state (string | null). Passed down to ChannelList and message area.
       - Full height: h-[calc(100vh-64px)] or similar to account for any top header
       - Message area placeholder: when no channel selected, show centered "Select a channel to start messaging" with MessageSquare icon. When channel selected, render a placeholder div with data-channel-id attribute (Plan 02 will replace this with the actual message list).

    4. Create `src/components/messaging/ChannelList.tsx` (per D-04):
       - Calls useChannels() hook to fetch channels
       - Groups channels by type: "Channels" (PUBLIC + PRIVATE) and "Direct Messages" (DM + GROUP_DM)
       - Each group has a section header (text-xs font-semibold uppercase text-slate-500 px-3 py-2)
       - Renders ChannelListItem for each channel
       - Skeleton loading during fetch: 8 skeleton items matching the ChannelListItem shape (per D-08)
       - Receives activeChannelId and onSelectChannel props

    5. Create `src/components/messaging/ChannelListItem.tsx` (per D-04, D-05):
       - Shows: channel name (# prefix for public, lock icon for private, user avatar for DMs), last message preview (truncated to 1 line), unread count badge, relative timestamp
       - Active state: bg-primary-50 border-l-2 border-primary-500 (per D-05)
       - Inactive state: hover:bg-white/50 transition-colors duration-200
       - Unread badge: when unreadCount > 0, show count in a small bg-primary-500 text-white rounded-full badge
       - For DMs: show the other user's name and avatar. For group DMs: show comma-separated first names.
       - onClick calls onSelectChannel(channelId)
       - Touch-friendly: min-h-[56px] for adequate tap target
  </action>
  <verify>
    <automated>cd /Users/mkerley/Desktop/Linfield Test && npx tsc --noEmit --pretty 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "RealtimeProvider" src/app/messaging/layout.tsx
    - grep -q "MessagingShell" src/app/messaging/page.tsx
    - grep -q "activeChannelId" src/components/messaging/MessagingShell.tsx
    - grep -q "useChannels" src/components/messaging/ChannelList.tsx
    - grep -q "ui-glass" src/components/messaging/MessagingShell.tsx
    - grep -q "onSelectChannel" src/components/messaging/ChannelListItem.tsx
    - grep -q "unreadCount" src/components/messaging/ChannelListItem.tsx
  </acceptance_criteria>
  <done>
    Messaging page renders at /messaging with RealtimeProvider wrapper. Two-column layout shows channel list sidebar (~280px) with grouped channels and a message area placeholder. Channel selection updates activeChannelId without page navigation. Skeleton loading displays during initial channel fetch.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> /api/messaging/channels | Channel list fetched via authenticated API call |
| Sidebar unread count | Derived from channel data, no separate unauthenticated endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Spoofing | Channel list fetch | mitigate | All API calls use credentials: 'include' for httpOnly cookie auth; middleware validates JWT |
| T-26-02 | Information Disclosure | Channel list | mitigate | GET /api/messaging/channels already filters by org-scoped Prisma + channel membership; UI only displays what API returns |
| T-26-03 | Elevation of Privilege | messagingEnabled gate | mitigate | Sidebar uses useModuleEnabled('messaging') client-side; API routes check assertMessagingEnabled server-side (defense in depth) |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes with no errors in new files
2. Sidebar shows Messaging nav item (visually verify in browser)
3. /messaging route renders the two-column layout
4. Channel list populates with channels from the API
</verification>

<success_criteria>
- Messaging page accessible at /messaging
- Channel list sidebar renders at ~280px width
- Sidebar nav item visible when messagingEnabled is true, hidden when false
- Unread badge shows total unread count across all channels
- Clicking a channel updates active state without navigation
</success_criteria>

<output>
After completion, create `.planning/phases/26-core-messaging-ui/26-01-SUMMARY.md`
</output>
