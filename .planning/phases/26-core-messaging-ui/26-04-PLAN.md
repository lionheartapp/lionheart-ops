---
phase: 26-core-messaging-ui
plan: 04
type: execute
wave: 3
depends_on: [26-02, 26-03]
files_modified:
  - src/components/messaging/ThreadPanel.tsx
  - src/components/messaging/MessagingShell.tsx
  - src/components/messaging/MessageArea.tsx
  - src/components/messaging/ChannelList.tsx
  - src/components/messaging/Composer.tsx
autonomous: false
requirements:
  - UI-04
  - UI-06

must_haves:
  truths:
    - "Clicking 'Reply in thread' on a message opens a thread panel from the right"
    - "Thread panel shows the parent message at top and thread replies below"
    - "User can send a reply in the thread panel"
    - "On mobile (<768px), channel list is a slide-over overlay"
    - "On mobile, message area fills the full screen"
    - "On mobile, thread panel replaces the message area"
  artifacts:
    - path: "src/components/messaging/ThreadPanel.tsx"
      provides: "Thread reply panel with parent message and replies"
  key_links:
    - from: "src/components/messaging/ThreadPanel.tsx"
      to: "/api/messaging/channels/[id]/messages?parentId=X"
      via: "useMessages with parentId filter"
      pattern: "parentId"
    - from: "src/components/messaging/MessagingShell.tsx"
      to: "ThreadPanel"
      via: "activeThreadId state"
      pattern: "activeThreadId"
---

<objective>
Thread panel and mobile-responsive layout.

Purpose: Adds the thread reply panel that slides in from the right for replying to specific messages, and makes the entire messaging layout responsive for mobile devices with overlay channel list and full-screen message area.

Output: Working thread panel with replies, and a mobile layout where the channel list is an overlay and the message area fills the screen.
</objective>

<execution_context>
@/Users/mkerley/Desktop/Linfield Test/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mkerley/Desktop/Linfield Test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-core-messaging-ui/26-CONTEXT.md
@.planning/phases/26-core-messaging-ui/26-01-SUMMARY.md
@.planning/phases/26-core-messaging-ui/26-02-SUMMARY.md
@.planning/phases/26-core-messaging-ui/26-03-SUMMARY.md

<interfaces>
From Plan 01 (MessagingShell):
```typescript
// MessagingShell manages activeChannelId state
// Plan 04 adds activeThreadId state and mobile overlay state
```

From Plan 02 (MessageArea):
```typescript
// MessageArea accepts onThreadClick prop
// Plan 04 wires this to open ThreadPanel
```

From Plan 02 (useMessages):
```typescript
// useMessages(channelId) — can also fetch thread replies by filtering parentId
// GET /api/messaging/channels/[id]/messages?parentId=msgId&limit=50
```

From Plan 03 (Composer):
```typescript
// Composer accepts parentId prop for thread replies
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread panel component</name>
  <files>
    src/components/messaging/ThreadPanel.tsx
  </files>
  <read_first>
    src/components/messaging/MessageArea.tsx,
    src/components/messaging/MessageBubble.tsx,
    src/components/messaging/Composer.tsx,
    src/lib/hooks/useMessages.ts
  </read_first>
  <action>
    1. Create `src/components/messaging/ThreadPanel.tsx` (per D-12, D-13):
       - Props: channelId (string), parentMessageId (string), parentMessage (MessageWithAuthor), onClose (() => void)
       - Layout: ~400px wide panel that slides in from the right with framer-motion (initial={{ x: '100%' }} animate={{ x: 0 }} exit={{ x: '100%' }} transition={{ type: 'tween', duration: 0.2 }})
       - Structure:
         - Header: "Thread" title + X close button (top bar, border-b border-slate-200, px-4 py-3)
         - Parent message: renders MessageBubble for the parent message at the top, with a subtle bottom border separator
         - Replies: uses a simplified version of the message list pattern — fetch replies via GET /api/messaging/channels/{channelId}/messages?parentId={parentMessageId}. Use useMessages hook if it supports parentId filtering, otherwise create a separate `useThreadReplies(channelId, parentId)` TanStack query.
         - Composer: renders Composer component at the bottom with parentId={parentMessageId} so replies are threaded
       - Panel background: bg-white border-l border-slate-200 h-full flex flex-col
       - Uses AnimatePresence for enter/exit animation
       - Keyboard: Escape key closes the panel
       - Realtime: thread replies should also appear in real time. The useRealtimeChannel hook's incomingMessage includes parentId — if parentId matches the active thread, append to thread replies list.
  </action>
  <verify>
    <automated>cd /Users/mkerley/Desktop/Linfield Test && npx tsc --noEmit --pretty 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "ThreadPanel" src/components/messaging/ThreadPanel.tsx
    - grep -q "parentMessageId\|parentMessage" src/components/messaging/ThreadPanel.tsx
    - grep -q "Composer" src/components/messaging/ThreadPanel.tsx
    - grep -q "onClose" src/components/messaging/ThreadPanel.tsx
    - grep -q "framer-motion\|motion" src/components/messaging/ThreadPanel.tsx
  </acceptance_criteria>
  <done>
    Thread panel slides in from the right showing parent message, thread replies, and a composer for thread replies. Escape key closes it. Framer-motion animates entry/exit.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire thread panel and add mobile responsive layout</name>
  <files>
    src/components/messaging/MessagingShell.tsx,
    src/components/messaging/MessageArea.tsx,
    src/components/messaging/ChannelList.tsx
  </files>
  <read_first>
    src/components/messaging/MessagingShell.tsx,
    src/components/messaging/MessageArea.tsx,
    src/components/messaging/ChannelList.tsx,
    src/components/messaging/ThreadPanel.tsx
  </read_first>
  <action>
    1. Update `src/components/messaging/MessagingShell.tsx` for thread panel (per D-12):
       - Add `activeThreadId` state (string | null) and `activeThreadMessage` state (MessageWithAuthor | null)
       - When onThreadClick fires from MessageArea: set activeThreadId and activeThreadMessage, fetch the parent message details
       - Render ThreadPanel inside AnimatePresence when activeThreadId is set
       - On desktop: ThreadPanel overlays to the right of the message area (position absolute or flex, does NOT shrink the message area per D-12)
       - On close: set activeThreadId to null

    2. Update `src/components/messaging/MessagingShell.tsx` for mobile layout (per D-16, D-17):
       - Use a `useIsMobile()` check (simple: `window.innerWidth < 768` with resize listener, or use Tailwind's md: breakpoint approach)
       - Mobile state machine with 3 views: 'channels' | 'messages' | 'thread'
       - Mobile channel list:
         - Renders as a full-screen overlay (fixed inset-0 z-40 bg-white) instead of the 280px sidebar
         - Slide-in from left with framer-motion
         - Shows a header with org name and a close button
         - When a channel is selected: transition to 'messages' view
       - Mobile message area:
         - Fills the full screen
         - Has a back button (ChevronLeft icon) in the header that returns to channel list view
         - Header shows channel name
       - Mobile thread panel (per D-13):
         - Replaces the message area entirely (not an overlay)
         - Has a back button that returns to message view
       - Touch targets: all buttons min-h-[44px] min-w-[44px] per D-17
       - Desktop layout unchanged — the existing flex layout works as-is for md: and above

    3. Update `src/components/messaging/MessageArea.tsx`:
       - Wire onThreadClick prop to actually call the parent's handler (was a no-op placeholder)
       - Pass it through to MessageList and then to individual MessageBubble components
  </action>
  <verify>
    <automated>cd /Users/mkerley/Desktop/Linfield Test && npx tsc --noEmit --pretty 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "activeThreadId" src/components/messaging/MessagingShell.tsx
    - grep -q "ThreadPanel" src/components/messaging/MessagingShell.tsx
    - grep -q "AnimatePresence" src/components/messaging/MessagingShell.tsx
    - grep -q "768\|isMobile\|useIsMobile" src/components/messaging/MessagingShell.tsx
    - grep -q "channels.*messages.*thread\|mobileView" src/components/messaging/MessagingShell.tsx
    - grep -q "ChevronLeft\|back" src/components/messaging/MessagingShell.tsx
  </acceptance_criteria>
  <done>
    Thread panel opens when clicking "Reply in thread" on a message. On mobile (<768px), channel list is a full-screen overlay, message area fills the screen with back button, and thread panel replaces message area. All touch targets are 44px minimum.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete messaging UI: sidebar nav item with unread badge, channel list, virtual-scrolled message list with realtime updates, composer with emoji and @mentions, thread panel, and mobile responsive layout.
  </what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev` (port 3004)
    2. Log in and check sidebar — "Messaging" should appear with unread badge if there are unread messages
    3. Click "Messaging" — should see channel list on left (~280px), message area on right
    4. Click a channel — messages load at the bottom with virtual scroll
    5. Scroll up — older messages load without scroll position jumping
    6. Type a message and press Enter — message sends and appears
    7. Press Shift+Enter — newline inserted
    8. Click emoji button — picker appears, select one — emoji inserted
    9. Type @  — autocomplete shows org members
    10. Click "replies" on a message with replies — thread panel slides in from right
    11. Resize browser to < 768px — channel list becomes overlay, message area fills screen
    12. On mobile view, tap back button — returns to channel list
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Thread replies -> /api/messaging/channels/[id]/messages?parentId=X | Thread data fetched via authenticated API |
| Mobile overlay | Client-side layout change, no new data boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-12 | Information Disclosure | Thread replies | mitigate | Thread reply endpoint uses same channel membership + org-scope checks as main message list |
| T-26-13 | Denial of Service | Mobile layout thrashing | accept | Resize listener debounced; layout switches are pure CSS/state changes with no API calls |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes
2. Thread panel opens with parent message and replies
3. Thread replies send correctly with parentId
4. Mobile layout shows overlay channel list
5. Mobile message area has back button navigation
6. Human verification checkpoint passes
</verification>

<success_criteria>
- Thread panel slides in from right with parent message + replies + composer
- Thread replies send with parentId and appear in real time
- Mobile: channel list is overlay, message area is full screen
- Mobile: thread panel replaces message area
- All touch targets minimum 44px
- Human verification checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/26-core-messaging-ui/26-04-SUMMARY.md`
</output>
