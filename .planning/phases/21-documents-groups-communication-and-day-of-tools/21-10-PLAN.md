---
phase: 21-documents-groups-communication-and-day-of-tools
plan: 10
type: execute
wave: 4
depends_on: ["21-03", "21-05", "21-07", "21-09"]
files_modified:
  - src/components/events/EventProjectTabs.tsx
  - src/app/events/[id]/dayof/page.tsx
  - src/middleware.ts
  - scripts/smoke-phase21.mjs
autonomous: false
requirements: [DOC-01, DOC-02, DOC-03, GRP-01, GRP-02, GRP-03, GRP-04, GRP-05, GRP-06, QR-01, QR-02, QR-03, QR-04, QR-05, COM-01, COM-02, COM-04, COM-05]

must_haves:
  truths:
    - "All 3 placeholder tabs (Documents, Logistics, Comms) are replaced with functional implementations"
    - "Day-of dashboard is accessible from the event project page"
    - "All public routes are properly whitelisted in middleware"
    - "All 19 requirements are addressable via the UI and API"
  artifacts:
    - path: "src/components/events/EventProjectTabs.tsx"
      provides: "Updated tab wiring with day-of dashboard link"
    - path: "src/app/events/[id]/dayof/page.tsx"
      provides: "Day-of operations page route"
    - path: "src/middleware.ts"
      provides: "All Phase 21 public paths whitelisted"
    - path: "scripts/smoke-phase21.mjs"
      provides: "Smoke tests covering key API endpoints"
  key_links:
    - from: "src/components/events/EventProjectTabs.tsx"
      to: "src/app/events/[id]/dayof/page.tsx"
      via: "Day-of button links to dayof page"
      pattern: "dayof|day-of"
    - from: "src/middleware.ts"
      to: "src/app/api/events/check-in"
      via: "Public path whitelist"
      pattern: "events/check-in"
---

<objective>
Wire everything together — ensure EventProjectTabs renders functional tabs (not placeholders), create the day-of dashboard page route, update middleware for all public paths, create smoke tests, and run a human verification checkpoint.

Purpose: This is the integration and verification plan that ensures all Phase 21 features are accessible, wired correctly, and pass a basic smoke test.
Output: Updated EventProjectTabs, day-of page, middleware updates, smoke tests, human verification.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-documents-groups-communication-and-day-of-tools/21-CONTEXT.md
@src/components/events/EventProjectTabs.tsx
@src/middleware.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire day-of page, update middleware, update EventProjectTabs, and create smoke tests</name>
  <files>src/app/events/[id]/dayof/page.tsx, src/middleware.ts, src/components/events/EventProjectTabs.tsx, scripts/smoke-phase21.mjs</files>
  <action>
**src/app/events/[id]/dayof/page.tsx:**
Create the day-of operations page:
- Server component that fetches the event project and renders DayOfDashboard.
- Route: /events/{id}/dayof.
- Auth required (staff page). Uses getUserContext and assertCan(EVENTS_CHECKIN_MANAGE).
- Imports and renders DayOfDashboard with eventProjectId.
- Back link to event project page.

**src/middleware.ts:**
Add any missing public paths for Phase 21:
- `/api/events/check-in/` — participant self-service API (QR-03).
- `/events/check-in/` — participant self-service page (QR-03).
- `/api/registration/*/announcements` — parent portal announcements. Check if `/api/registration/` is already covered (it should be from Phase 20 — confirm and add if not).
- `/api/events/projects/*/surveys/*/responses` — public survey response submission. Note: this needs careful handling — only POST should be public. If the middleware does not support method-based filtering, add the path and handle auth within the route itself (POST is public, GET requires auth).

**src/components/events/EventProjectTabs.tsx:**
Verify and update if needed:
- Ensure EventDocumentsTab, EventLogisticsTab, and EventCommsTab imports point to the updated components (they should already since plans 03, 05, 07 replaced the files in-place).
- Add a "Day-Of" action button in the tab bar or as a floating button. This is NOT a tab — it's a link to /events/${project.id}/dayof. Show it as a prominent button: "Launch Day-Of Mode" with a QR scan icon, using the dark pill button style. Only show when event status is IN_PROGRESS or when startsAt is within 24 hours.
- Confirm the PresenceBar (from Plan 07) is rendering correctly above the tabs.

**scripts/smoke-phase21.mjs:**
Create a smoke test file following the existing smoke test pattern (scripts/smoke-registration.mjs):
- Test structure: runTest helper, TESTS registry, --test= CLI arg.
- Test cases (stubs that log SKIP initially — will be filled in during manual testing or future CI):

1. `doc-requirements-crud` — POST/GET/DELETE document requirement.
2. `doc-completion-matrix` — GET document completion matrix.
3. `doc-compliance-crud` — POST/GET/DELETE compliance item.
4. `group-crud` — POST/GET/DELETE group.
5. `group-assignment` — POST/DELETE group assignment.
6. `group-auto-assign` — POST auto-assign.
7. `activity-crud` — POST/GET/DELETE activity.
8. `activity-signup` — POST/DELETE activity signup.
9. `dietary-medical-report` — GET dietary/medical report.
10. `announcement-crud` — POST/GET/DELETE announcement.
11. `survey-crud` — POST/GET/DELETE survey.
12. `check-in` — POST check-in.
13. `check-in-counter` — GET check-in counter.
14. `incident-crud` — POST/GET incident.
15. `participant-self-service` — GET /api/events/check-in/${regId} (public).
16. `presence-heartbeat` — POST/GET/DELETE presence.
17. `parent-announcements` — GET /api/registration/${regId}/announcements (public).
18. `parent-groups` — GET /api/registration/${regId}/groups (public).

Each test stub: logs test name, logs "SKIP — not implemented", returns { pass: true }.
  </action>
  <verify>
    <automated>cd "/Users/mkerley/Desktop/Linfield Test" && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Day-of page route exists, middleware updated, EventProjectTabs has day-of button, smoke test file created with 18 test stubs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of Phase 21 integration</name>
  <files>N/A</files>
  <action>
Pause for human verification of the complete Phase 21 feature set. Present the following verification steps to the user:

1. Start dev server: `npm run dev` (port 3004)
2. Navigate to an existing event project page
3. Verify Documents tab shows 3 sub-tabs (Requirements, Completion, Compliance) instead of placeholder
4. Verify Logistics tab shows group type sub-tabs (Buses, Cabins, etc.) instead of placeholder
5. Verify Comms tab shows Announcements and Surveys sub-tabs instead of placeholder
6. Verify presence bar appears above tabs showing current user avatar
7. Click "Launch Day-Of Mode" button — should navigate to /events/{id}/dayof
8. On day-of page, verify QR scanner viewfinder appears (may need camera permission)
9. Check that the smoke test file runs: `node scripts/smoke-phase21.mjs` (should show all 18 tests as SKIP)
10. Visit /events/check-in/any-registration-id — should show 404 or participant info (if valid ID)
  </action>
  <verify>User confirms "approved" or provides issues to fix</verify>
  <done>Human has verified all Phase 21 features are visually and functionally integrated.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm run dev` starts without errors
- All 3 former placeholder tabs show functional content
- Day-of page accessible at /events/[id]/dayof
- Smoke tests run: `node scripts/smoke-phase21.mjs`
- No broken imports or missing dependencies
</verification>

<success_criteria>
- All 19 Phase 21 requirements are addressed across the feature set
- No placeholder stubs remain in the Documents, Logistics, or Comms tabs
- Day-of dashboard is accessible and functional
- Offline PWA support is wired (Dexie tables, service worker rules)
- Human verification confirms the visual and functional integrity
</success_criteria>

<output>
After completion, create `.planning/phases/21-documents-groups-communication-and-day-of-tools/21-10-SUMMARY.md`
</output>
