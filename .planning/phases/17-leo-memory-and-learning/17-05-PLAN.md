---
phase: 17-leo-memory-and-learning
plan: 05
type: execute
wave: 3
depends_on: ["17-02"]
files_modified:
  - src/components/ai/ChatPanel.tsx
  - src/components/ai/ConversationSidebar.tsx
  - src/components/ai/MessageList.tsx
autonomous: true
requirements:
  - LEO-MEM-01
  - LEO-MEM-02

must_haves:
  truths:
    - "Users see a list of past conversations in Leo's chat UI and can click to load one"
    - "Clicking a past conversation loads its full message history into the chat panel"
    - "Users can click thumbs up or thumbs down on any assistant message"
    - "Feedback button state reflects persisted feedback (filled icon after clicking)"
    - "Starting a new conversation clears the chat and creates a fresh session"
    - "The conversation list shows title, date, and message count"
  artifacts:
    - path: "src/components/ai/ConversationSidebar.tsx"
      provides: "Conversation list component with load/delete actions"
    - path: "src/components/ai/ChatPanel.tsx"
      provides: "Updated chat panel with conversation sidebar toggle and conversationId tracking"
    - path: "src/components/ai/MessageList.tsx"
      provides: "Updated message list with thumbs up/down feedback buttons"
  key_links:
    - from: "src/components/ai/ChatPanel.tsx"
      to: "/api/conversations"
      via: "fetch to load conversation list"
      pattern: "fetch.*api/conversations"
    - from: "src/components/ai/ChatPanel.tsx"
      to: "/api/conversations/[id]/messages"
      via: "fetch to load conversation messages"
      pattern: "fetch.*api/conversations.*messages"
    - from: "src/components/ai/MessageList.tsx"
      to: "/api/conversations/[id]/feedback"
      via: "fetch to submit thumbs up/down"
      pattern: "fetch.*feedback"
---

<objective>
Build the frontend UI for conversation history (sidebar list, load past conversations) and message feedback (thumbs up/down).

Purpose: Users can resume past Leo conversations and provide feedback on responses, closing the loop on LEO-MEM-01 and LEO-MEM-02.
Output: Conversation sidebar component, updated ChatPanel with history management, feedback buttons on messages.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-leo-memory-and-learning/17-02-SUMMARY.md
@src/components/ai/ChatPanel.tsx
@src/components/ai/MessageList.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create conversation sidebar component</name>
  <files>src/components/ai/ConversationSidebar.tsx</files>
  <action>
Create `src/components/ai/ConversationSidebar.tsx` — a sliding panel that shows past conversations.

**Props:**
```typescript
interface ConversationSidebarProps {
  isOpen: boolean
  onClose: () => void
  onSelectConversation: (conversationId: string) => void
  onNewConversation: () => void
  activeConversationId: string | null
}
```

**Design:**
- Sliding panel from the left side of the chat panel (not a separate modal)
- Width: 260px, overlays the chat panel content with `ui-glass-overlay` styling
- AnimatePresence + motion.div for slide-in/out animation (slide from left, 200ms)
- Background: white with slight blur (matches chat panel glass style)
- Header: "Conversations" title + "New Chat" button (aurora gradient pill, small)
- List: scrollable, each item shows:
  - Title (truncated to 1 line, max 30 chars) or "Untitled" if null
  - Relative date: "Today", "Yesterday", "Mar 8", etc.
  - Message count badge (small, gray-100 pill)
  - Active state: aurora-gradient left border (2px) + bg-primary-50
  - Hover: bg-gray-50 transition-colors
  - Click handler: calls onSelectConversation(id)
- Delete button: small X icon on hover (right side), with confirmation via a simple "Delete?" text toggle (not a modal)
- Group conversations by: "Today", "Yesterday", "This Week", "Earlier" sections with small gray labels
- Loading state: 3 skeleton items (animate-pulse)
- Empty state: "No conversations yet" centered text with subtle icon

**Data fetching:**
- On mount (isOpen=true), fetch `GET /api/conversations?limit=30` with `credentials: 'include'`
- Cache the result in state, refetch when isOpen transitions to true
- After delete, remove from local state optimistically then `DELETE /api/conversations/[id]`

**Styling follows project conventions:**
- Rounded corners, clean borders, Tailwind only
- `cursor-pointer` on all clickable elements
- `transition-colors duration-200` on interactive elements
- No emojis
  </action>
  <verify>
    <automated>cd "/Users/mkerley/Desktop/Linfield Test" && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>ConversationSidebar.tsx renders a sliding panel with grouped conversation list, load-on-click, delete with optimistic removal, skeleton loading state, and empty state. Follows project glass/aurora styling conventions.</done>
</task>

<task type="auto">
  <name>Task 2: Wire conversation sidebar and feedback into ChatPanel and MessageList</name>
  <files>src/components/ai/ChatPanel.tsx, src/components/ai/MessageList.tsx</files>
  <action>
**Update `src/components/ai/ChatPanel.tsx`:**

1. Add state for conversation management:
   - `const [conversationId, setConversationId] = useState<string | null>(null)`
   - `const [showSidebar, setShowSidebar] = useState(false)`

2. Add a history toggle button in the header (between the Leo name and the clear/close buttons):
   - Icon: `Clock` from lucide-react (small, matches existing button size)
   - `onClick={() => setShowSidebar(prev => !prev)}`
   - Tooltip: "Conversation history"
   - Same styling as the existing RotateCcw button

3. Import and render `ConversationSidebar`:
   - Place inside the main panel div, before MessageList
   - Pass `isOpen={showSidebar}`, `onClose={() => setShowSidebar(false)}`
   - `onSelectConversation`: async handler that:
     a. Fetches `GET /api/conversations/${id}/messages` with `credentials: 'include'`
     b. Maps messages to ConversationTurn format (role, content, timestamp, messageId, feedbackScore)
     c. Sets conversation state and conversationId
     d. Closes sidebar
   - `onNewConversation`: calls existing `handleClearChat` + `setConversationId(null)` + closes sidebar
   - `activeConversationId={conversationId}`

4. Track conversationId from SSE:
   - In the SSE event handler switch, add a case for `'conversation_id'`:
     ```
     case 'conversation_id':
       setConversationId(event.conversationId)
       break
     ```

5. Send conversationId in chat request:
   - In `handleSendMessage`, add `conversationId` to the fetch body:
     ```
     body: JSON.stringify({
       message,
       conversationHistory: conversation,
       conversationId: conversationId || undefined,
       ...(images...)
     })
     ```

6. Update `handleClearChat` to also reset `conversationId`:
   ```
   setConversationId(null)
   ```

7. Add `onFeedback` callback for MessageList:
   ```
   const handleFeedback = useCallback(async (messageId: string, score: number) => {
     if (!conversationId) return
     try {
       await fetch(`/api/conversations/${conversationId}/feedback`, {
         method: 'POST',
         headers: { 'Content-Type': 'application/json' },
         credentials: 'include',
         body: JSON.stringify({ messageId, score }),
       })
       // Update local conversation state to reflect feedback
       setConversation(prev => prev.map(turn =>
         turn.messageId === messageId ? { ...turn, feedbackScore: score } : turn
       ))
     } catch { /* silent */ }
   }, [conversationId])
   ```

**Update `src/components/ai/MessageList.tsx`:**

1. Add `onFeedback?: (messageId: string, score: number) => void` to props

2. For each assistant message, add thumbs up/down buttons below the message text:
   - Only show on assistant messages (role === 'assistant')
   - Only show if the message has a `messageId` (persisted messages)
   - Two small icon buttons: `ThumbsUp` and `ThumbsDown` from lucide-react
   - Size: h-3.5 w-3.5, same scale as tool status icons
   - Default state: text-gray-300 hover:text-gray-500
   - Active thumbs up (feedbackScore === 5): text-green-500
   - Active thumbs down (feedbackScore === 1): text-red-400
   - Clicking calls `onFeedback(messageId, score)`
   - If already selected, clicking the same button deselects (sends null/0)
   - Position: right-aligned below the message content, small gap (mt-1)
   - Subtle appearance — don't make them visually dominant
   - `transition-colors duration-150` on both buttons

3. The buttons should only appear on hover of the message bubble (or always visible on the last message):
   - Use `group` class on the message container
   - `opacity-0 group-hover:opacity-100 transition-opacity` on the feedback container
   - Last assistant message: always visible (no hover needed)
  </action>
  <verify>
    <automated>cd "/Users/mkerley/Desktop/Linfield Test" && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>ChatPanel.tsx has conversation history sidebar toggle, tracks conversationId via SSE, sends conversationId with each message, and passes feedback handler to MessageList. MessageList.tsx shows thumbs up/down on assistant messages with hover reveal and active state styling. Feedback persisted via POST /api/conversations/[id]/feedback.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. ConversationSidebar.tsx renders conversation list with proper grouping and interactions
3. ChatPanel.tsx includes sidebar toggle button, handles conversation_id SSE event, sends conversationId
4. MessageList.tsx renders feedback buttons on assistant messages
5. Feedback calls POST /api/conversations/[id]/feedback correctly
6. Loading a past conversation populates the chat with historical messages
</verification>

<success_criteria>
- User can open conversation history sidebar from Leo's header
- Past conversations are listed with title, date, message count
- Clicking a conversation loads its messages into the chat
- Thumbs up/down buttons appear on assistant messages
- Feedback is persisted to the database and visually reflected
- "New Chat" button starts a fresh conversation
</success_criteria>

<output>
After completion, create `.planning/phases/17-leo-memory-and-learning/17-05-SUMMARY.md`
</output>
