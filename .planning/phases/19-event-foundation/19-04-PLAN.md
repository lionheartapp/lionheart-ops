---
phase: 19-event-foundation
plan: 04
type: execute
wave: 4
depends_on: [19-03]
files_modified:
  - src/components/Sidebar.tsx
  - src/app/events/page.tsx
  - src/app/events/[id]/page.tsx
  - src/components/events/EventProjectTabs.tsx
  - src/components/events/EventOverviewTab.tsx
  - src/components/events/EventPeopleTab.tsx
  - src/components/events/EventDocumentsTab.tsx
  - src/components/events/EventLogisticsTab.tsx
  - src/components/events/EventBudgetTab.tsx
  - src/components/events/EventCommsTab.tsx
  - src/components/events/EventActivityLog.tsx
  - src/components/events/CreateEventProjectModal.tsx
  - src/lib/hooks/useEventProject.ts
autonomous: false
requirements: [EVNT-04, EVNT-09]

must_haves:
  truths:
    - "Events appears as a top-level sidebar item above all other nav items"
    - "Clicking Events opens a secondary panel showing Calendar, Planning, and active EventProjects"
    - "Navigating to /calendar or /planning auto-opens the Events secondary panel"
    - "Existing /calendar and /planning URLs continue working without redirects"
    - "Events list page shows all EventProjects with status chips and create button"
    - "EventProject detail page shows 8 tabs: Overview, Schedule, People, Documents, Logistics, Budget, Tasks, Comms"
    - "Empty tab states show a descriptive message and icon serving as a built-in checklist"
    - "Activity log displays chronological entries with actor name, type, and timestamp"
  artifacts:
    - path: "src/components/Sidebar.tsx"
      provides: "Events as top-level nav with Calendar+Planning nested in secondary panel"
      contains: "eventsOpen"
    - path: "src/app/events/page.tsx"
      provides: "Events list page with project cards"
      min_lines: 30
    - path: "src/app/events/[id]/page.tsx"
      provides: "EventProject workspace with 8-tab layout"
      min_lines: 30
    - path: "src/components/events/EventProjectTabs.tsx"
      provides: "8-tab container with aurora gradient tab indicator"
      contains: "Overview"
    - path: "src/lib/hooks/useEventProject.ts"
      provides: "TanStack Query hooks for event project data"
      exports: ["useEventProject", "useEventProjects"]
  key_links:
    - from: "src/components/Sidebar.tsx"
      to: "src/app/events/page.tsx"
      via: "Events nav item links to /events"
      pattern: "href.*events"
    - from: "src/app/events/[id]/page.tsx"
      to: "src/lib/hooks/useEventProject.ts"
      via: "useEventProject hook fetches project data"
      pattern: "useEventProject"
    - from: "src/lib/hooks/useEventProject.ts"
      to: "/api/events/projects"
      via: "TanStack Query fetch"
      pattern: "api/events/projects"
---

<objective>
Refactor the sidebar to show Events as the primary navigation item with Calendar and Planning nested under it, create the Events list page, the EventProject 8-tab workspace page with empty states for future-phase tabs, activity log component, and TanStack Query hooks.

Purpose: This is the main UI that staff interact with. Events becomes the top-level navigation concept ("Events Are the Product"), and the 8-tab workspace is where all event management happens. Empty tab states serve as a built-in checklist showing staff what's possible.
Output: Refactored sidebar, events list page, event detail page with 8 tabs, activity log component, create modal, TanStack Query hooks.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-event-foundation/19-RESEARCH.md
@.planning/phases/19-event-foundation/19-03-SUMMARY.md

@src/components/Sidebar.tsx
@src/lib/animations.ts
@src/app/dashboard/page.tsx

<interfaces>
<!-- API routes from Plan 03 -->

GET /api/events/projects — returns { ok: true, data: EventProject[], meta: PaginationMeta }
POST /api/events/projects — body: CreateEventProjectSchema -> { ok: true, data: EventProject }
GET /api/events/projects/[id] — returns { ok: true, data: EventProject (with scheduleBlocks, tasks, activityLog, createdBy) }
PATCH /api/events/projects/[id] — body: UpdateEventProjectSchema -> { ok: true, data: EventProject }
GET /api/events/projects/[id]/activity — returns { ok: true, data: EventActivityLog[] }
POST /api/events/projects/[id]/approve — returns { ok: true, data: EventProject }

<!-- Sidebar patterns (from codebase) -->
- athleticsOpen / handleAthleticsClick / athleticsNavContent pattern
- calendarOpen / calendarNavContent pattern
- secondaryOpen = settingsOpen || calendarOpen || athleticsOpen
- navItems array with icon, label, href

<!-- UI conventions (from MEMORY.md) -->
- Card style: ui-glass p-6 or bg-white border border-gray-200 rounded-xl p-6
- Aurora gradient for tab indicators: linear-gradient(90deg, #3B82F6, #6366F1)
- Skeleton loading: animate-pulse matching final layout
- Empty states: centered icon in rounded-2xl bg-primary-50 container + heading + description
- Framer Motion: fadeInUp, cardEntrance from src/lib/animations.ts
- Button styles: dark pill primary, ghost pill secondary
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor sidebar to nest Calendar and Planning under Events and create TanStack hooks</name>
  <files>src/components/Sidebar.tsx, src/lib/hooks/useEventProject.ts</files>
  <action>
    1. Create `src/lib/hooks/useEventProject.ts`:
       - `useEventProjects(filters?)` — TanStack Query hook, fetches `GET /api/events/projects`. Returns `{ data, isLoading, error }`. Query key: `['event-projects', filters]`.
       - `useEventProject(id)` — Fetches `GET /api/events/projects/${id}`. Returns full project with schedule, tasks, activity. Query key: `['event-project', id]`. Enabled only when id is truthy.
       - `useCreateEventProject()` — useMutation for POST /api/events/projects. Invalidates `['event-projects']` on success.
       - `useUpdateEventProject(id)` — useMutation for PATCH. Invalidates both list and detail query keys.
       - `useApproveEventProject(id)` — useMutation for POST /api/events/projects/${id}/approve. Invalidates project detail.
       - `useEventActivity(id)` — Fetches GET /api/events/projects/${id}/activity. Query key: `['event-activity', id]`.
       - Follow existing hook patterns in the codebase (check `src/lib/hooks/` for fetch helper patterns).

    2. Refactor `src/components/Sidebar.tsx`:

       a. **Add Events state** (near existing athleticsOpen/calendarOpen):
          ```typescript
          const [eventsOpen, setEventsOpen] = useState(false)
          ```

       b. **Move Calendar out of navItems**, replace with Events:
          - Remove `{ icon: Calendar, label: 'Calendar', href: '/calendar' }` from navItems
          - Add Events as the SECOND item (after Dashboard): `{ icon: CalendarClock, label: 'Events', href: '/events' }`
          - Import `CalendarClock` from lucide-react (or use a suitable event icon)

       c. **Create handleEventsClick** following handleAthleticsClick pattern:
          ```typescript
          const handleEventsClick = () => {
            if (!eventsOpen) {
              setEventsOpen(true)
              setSettingsOpen(false)
              setAthleticsOpen(false)
              setCalendarOpen(false) // Close calendar since it's now under Events
            } else {
              setEventsOpen(false)
            }
          }
          ```

       d. **Update secondaryOpen**:
          ```typescript
          const secondaryOpen = settingsOpen || calendarOpen || athleticsOpen || eventsOpen
          ```
          IMPORTANT: Keep `calendarOpen` in the expression for backward compatibility, but calendarOpen should now be triggered BY the Events panel's Calendar link, not by a separate nav item.

       e. **Auto-open Events panel on route detection** (in the useIsomorphicLayoutEffect or useEffect that detects pathname):
          ```typescript
          if (pathname.startsWith('/events') || pathname.startsWith('/calendar') || pathname.startsWith('/planning')) {
            setEventsOpen(true)
            setSettingsOpen(false)
            setAthleticsOpen(false)
          }
          ```
          Remove or adjust the existing calendar auto-open logic that opens calendarOpen directly. Instead, navigating to /calendar should open eventsOpen (the Events panel contains the calendar link).

       f. **Create eventsNavContent** — secondary panel content for Events:
          - Header: "Events" with a CalendarClock icon
          - Section 1: Navigation links
            - "Calendar" link to /calendar (highlighted when pathname starts with /calendar)
            - "Planning" link to /planning (highlighted when pathname starts with /planning)
          - Section 2: "Active Events" — show a list of active EventProjects (use TanStack Query to fetch, or show a simple link to /events). Keep this lightweight — just title + status chip for the first 5 active projects.
          - Section 3 (bottom): "Create Event" button linking to /events with a create modal trigger, or just a "+ New Event" button that navigates to /events (the create flow lives on the events page).
          - Move the existing Planning CTA card from calendarNavContent to the bottom of eventsNavContent (per research recommendation).

       g. **Update secondary panel render logic**:
          Currently: `{athleticsOpen ? athleticsNavContent : calendarOpen ? calendarNavContent : settingsNavContent}`
          Change to: `{eventsOpen ? eventsNavContent : athleticsOpen ? athleticsNavContent : calendarOpen ? calendarNavContent : settingsNavContent}`
          Apply this in BOTH desktop aside and mobile nav sections.

       h. **Update aria-label** on secondary panel:
          Add `eventsOpen ? 'Events navigation' :` before the existing ternary.

       i. **Update active state logic on nav items**:
          Currently items check `!settingsOpen && !athleticsOpen` for active highlighting.
          Add `!eventsOpen` to these checks so nav items de-highlight when Events panel is open.

    CRITICAL:
    - Do NOT remove /calendar or /planning routes — they stay. Only the sidebar nav structure changes.
    - The Calendar secondary panel (calendarNavContent with calendar checkboxes, mini-calendar, etc.) should still be accessible. When a user clicks "Calendar" inside the Events panel, it should navigate to /calendar. The calendarOpen state can still be used to show the full calendar sidebar content (checkboxes, etc.). Consider: clicking "Calendar" in the Events panel navigates to /calendar and also sets calendarOpen=true (showing the calendar-specific checkboxes in a sub-section or replacing eventsNavContent). OR: simplify by just navigating to /calendar — the existing calendar page has its own sidebar context.
    - Simplest approach: Events panel always shows when eventsOpen=true. Inside it, Calendar link navigates to /calendar. The calendar page itself can still trigger calendarOpen for the checkbox sidebar. This is a progressive approach that doesn't break existing calendar UX.
  </action>
  <verify>
    <automated>cd "/Users/mkerley/Desktop/Linfield Test" && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Sidebar shows Events as top-level nav item, secondary panel has Calendar/Planning/Active Events links, existing /calendar and /planning URLs still work, TypeScript compiles</done>
</task>

<task type="auto">
  <name>Task 2: Create Events list page, EventProject 8-tab workspace, and empty-state tab components</name>
  <files>src/app/events/page.tsx, src/app/events/[id]/page.tsx, src/components/events/EventProjectTabs.tsx, src/components/events/EventOverviewTab.tsx, src/components/events/EventPeopleTab.tsx, src/components/events/EventDocumentsTab.tsx, src/components/events/EventLogisticsTab.tsx, src/components/events/EventBudgetTab.tsx, src/components/events/EventCommsTab.tsx, src/components/events/EventActivityLog.tsx, src/components/events/CreateEventProjectModal.tsx</files>
  <action>
    1. Create `src/app/events/page.tsx` — Events list page:
       - Use `useEventProjects()` hook to fetch projects
       - Header: "Events" title with filter chips (All, Draft, Pending, Confirmed, In Progress, Completed)
       - "+ New Event" primary button (dark pill style) that opens CreateEventProjectModal
       - Project cards: ui-glass-hover style, show title, date range, status chip (color-coded), source badge (planning/series/direct), created by
       - Status chip colors: DRAFT=gray, PENDING_APPROVAL=yellow, CONFIRMED=blue, IN_PROGRESS=green, COMPLETED=purple, CANCELLED=red
       - Click card navigates to `/events/${project.id}`
       - Skeleton loading with animate-pulse matching card layout
       - Empty state: "No events yet" with descriptive text and Create Event CTA
       - Use Framer Motion stagger for card entrance

    2. Create `src/components/events/CreateEventProjectModal.tsx`:
       - Modal/drawer for creating a new direct-request EventProject
       - Fields: title (required), description (optional), start date/time, end date/time, is multi-day toggle, location text, building/room selectors (if campus data available), expected attendance
       - Uses `useCreateEventProject()` mutation
       - On success: invalidates query, closes modal, navigates to new project page
       - Zod validation on submit matching CreateEventProjectSchema
       - ui-glass-overlay styling for modal backdrop

    3. Create `src/components/events/EventProjectTabs.tsx`:
       - 8-tab container component that receives an EventProject as prop
       - Tabs: Overview, Schedule, People, Documents, Logistics, Budget, Tasks, Comms
       - Each tab has an icon (from lucide-react): LayoutDashboard, CalendarDays, Users, FileText, Truck, DollarSign, CheckSquare, MessageSquare
       - Tab indicator uses aurora gradient: `linear-gradient(90deg, #3B82F6, #6366F1)`
       - AnimatePresence for tab content switching (mode="wait", fade transition)
       - Renders the appropriate tab component based on active tab
       - Use URL hash or query param to persist active tab (so deep-links work)

    4. Create `src/app/events/[id]/page.tsx` — EventProject workspace:
       - Extract `id` from params
       - Use `useEventProject(id)` hook
       - Header: project title (editable inline), status chip, action buttons (Approve if PENDING_APPROVAL, Edit, etc.)
       - Below header: date range, location, created by
       - Main content: `<EventProjectTabs project={project} />`
       - Skeleton loading matching tab layout
       - 404 state if project not found

    5. Create `src/components/events/EventOverviewTab.tsx`:
       - Summary section: project details (description, dates, location, expected attendance)
       - Quick stats row: total tasks, completed tasks, schedule blocks count, days until event
       - Activity log preview (last 5 entries) with "View all" link to activity log tab
       - Status timeline showing project lifecycle stages

    6. Create empty-state tab components (these are shells for future phases):
       - `EventPeopleTab.tsx` — Empty state: "People & Registration" with Users icon. Description: "Registration forms, participant lists, and team assignments will appear here." Note: "Set up in Registration phase"
       - `EventDocumentsTab.tsx` — Empty state: "Documents & Signatures" with FileText icon. Description: "Permission slips, waivers, medical releases, and compliance tracking." Note: "Set up in Documents phase"
       - `EventLogisticsTab.tsx` — Empty state: "Logistics & Groups" with Truck icon. Description: "Bus assignments, cabin rosters, group management, and supply tracking." Note: "Set up in Groups phase"
       - `EventBudgetTab.tsx` — Empty state: "Budget & Expenses" with DollarSign icon. Description: "Line-item budgets, expense tracking, revenue logging, and cost analysis." Note: "Set up in Budget phase"
       - `EventCommsTab.tsx` — Empty state: "Communications" with MessageSquare icon. Description: "Announcements, email campaigns, notification timelines, and surveys." Note: "Set up in Communications phase"
       - Each empty state: centered icon in rounded-2xl bg-primary-50/80 container (48x48px), heading, description text, subtle "Coming soon" badge. Use fadeInUp animation on mount.

    7. Create `src/components/events/EventActivityLog.tsx`:
       - Receives eventProjectId as prop
       - Uses `useEventActivity(eventProjectId)` hook
       - Chronological list of activity entries (newest first)
       - Each entry: actor avatar/name, activity type badge (color-coded), description text derived from type + metadata, relative timestamp (date-fns formatDistanceToNow)
       - Activity type display: CREATED="created this event", STATUS_CHANGE="changed status from X to Y", TASK_CREATED="added task: {title}", SCHEDULE_BLOCK_ADDED="added schedule block: {title}", APPROVAL_GRANTED="approved this event", etc.
       - Framer Motion stagger on list items

    IMPORTANT:
    - All components use `'use client'` directive (they use hooks and interactivity)
    - Follow glassmorphism system: ui-glass for cards, ui-glass-hover for interactive cards
    - Skeleton loading during data fetch (animate-pulse blocks matching final layout)
    - Empty states are the "Show Everything" design decision — they show what's possible, not error states
    - Use existing animation patterns from src/lib/animations.ts (fadeInUp, cardEntrance, staggerContainer)
  </action>
  <verify>
    <automated>cd "/Users/mkerley/Desktop/Linfield Test" && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Events list page renders with project cards, project detail page shows 8-tab workspace, all tabs render (5 with empty states), activity log displays entries, TypeScript compiles</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Events navigation, list page, and 8-tab workspace</name>
  <files>src/components/Sidebar.tsx, src/app/events/page.tsx, src/app/events/[id]/page.tsx</files>
  <action>Human verification of the Events UI. Run dev server and visually inspect sidebar navigation refactor, events list page, 8-tab workspace, and backward compatibility of existing routes.</action>
  <verify>
    <automated>cd "/Users/mkerley/Desktop/Linfield Test" && npx tsc --noEmit --pretty 2>&1 | head -5</automated>
  </verify>
  <done>Sidebar shows Events as top-level with Calendar/Planning nested, events list and 8-tab workspace load correctly, existing routes preserved</done>
  <what-built>Events sidebar navigation refactor (Events as top-level with Calendar/Planning nested), Events list page with project cards and create modal, EventProject 8-tab workspace with Overview + Activity Log + 5 empty-state tabs</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3004
    2. Verify sidebar: Events should be the second item (after Dashboard), with a CalendarClock icon. Click Events and verify a secondary panel opens showing Calendar, Planning, and Active Events links.
    3. Click Calendar link in the Events panel — verify /calendar loads normally. Click Planning — verify /planning loads normally. Both should highlight within the Events panel.
    4. Navigate to /events — verify the events list page loads with filter chips and a "New Event" button. If no events exist, verify the empty state looks good.
    5. Click "New Event" — verify the create modal opens with title, dates, description fields. Create a test event. Verify it appears in the list.
    6. Click the test event card — verify the 8-tab workspace loads. Check each tab:
       - Overview: shows project details and quick stats
       - Schedule: (will be implemented in Plan 05)
       - People: shows empty state "People & Registration"
       - Documents: shows empty state "Documents & Signatures"
       - Logistics: shows empty state "Logistics & Groups"
       - Budget: shows empty state "Budget & Expenses"
       - Tasks: (will be implemented in Plan 05)
       - Comms: shows empty state "Communications"
    7. Verify the activity log shows at least a "created this event" entry.
    8. Verify all existing routes (/calendar, /planning, /dashboard, /maintenance, /athletics) still work.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Events appears in sidebar as top-level item
- /events page loads and shows EventProject list
- /events/[id] page loads with 8-tab layout
- /calendar and /planning URLs still work
- Activity log renders with formatted entries
- Empty state tabs show descriptive content (not error states)
</verification>

<success_criteria>
- Sidebar shows Events as primary navigation with Calendar+Planning nested in secondary panel
- Events list page with create modal, filter chips, project cards
- EventProject 8-tab workspace with Overview tab showing details and activity preview
- 5 empty-state tabs for future phases (People, Documents, Logistics, Budget, Comms)
- Activity log component showing chronological event history
- All existing routes preserved and working
</success_criteria>

<output>
After completion, create `.planning/phases/19-event-foundation/19-04-SUMMARY.md`
</output>
