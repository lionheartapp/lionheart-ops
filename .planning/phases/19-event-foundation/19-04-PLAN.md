---
phase: 19-event-foundation
plan: 04
type: execute
wave: 4
depends_on: [19-03]
files_modified:
  - src/components/Sidebar.tsx
  - src/lib/hooks/useEventProject.ts
  - src/lib/hooks/useEventSchedule.ts
  - src/lib/hooks/useEventTasks.ts
autonomous: true
requirements: [EVNT-09, EVNT-10]

must_haves:
  truths:
    - "Events appears as a top-level sidebar item above all other nav items"
    - "Clicking Events opens a secondary panel showing Dashboard, Calendar, Planning, and active EventProjects"
    - "Navigating to /calendar or /planning auto-opens the Events secondary panel"
    - "Existing /calendar and /planning URLs continue working without redirects"
    - "Dashboard link (/events/dashboard) is the first item in the Events secondary sidebar panel (before Calendar/Planning)"
    - "TanStack Query hooks export correctly and target the correct API endpoints"
  artifacts:
    - path: "src/components/Sidebar.tsx"
      provides: "Events as top-level nav with Dashboard+Calendar+Planning nested in secondary panel"
      contains: "eventsOpen"
    - path: "src/lib/hooks/useEventProject.ts"
      provides: "TanStack Query hooks for event project data"
      exports: ["useEventProject", "useEventProjects", "useCreateEventProject", "useUpdateEventProject", "useApproveEventProject", "useEventActivity"]
    - path: "src/lib/hooks/useEventSchedule.ts"
      provides: "TanStack Query hooks for schedule block CRUD"
      exports: ["useScheduleBlocks", "useCreateScheduleBlock", "useUpdateScheduleBlock", "useDeleteScheduleBlock"]
    - path: "src/lib/hooks/useEventTasks.ts"
      provides: "TanStack Query hooks for event task CRUD"
      exports: ["useEventTasks", "useCreateEventTask", "useUpdateEventTask", "useDeleteEventTask"]
  key_links:
    - from: "src/components/Sidebar.tsx"
      to: "/events"
      via: "Events nav item links to /events"
      pattern: "href.*events"
    - from: "src/components/Sidebar.tsx"
      to: "/events/dashboard"
      via: "Dashboard link in Events secondary panel"
      pattern: "events/dashboard"
    - from: "src/lib/hooks/useEventProject.ts"
      to: "/api/events/projects"
      via: "TanStack Query fetch"
      pattern: "api/events/projects"
    - from: "src/lib/hooks/useEventSchedule.ts"
      to: "/api/events/projects/[id]/schedule"
      via: "TanStack Query fetch"
      pattern: "api/events/projects"
    - from: "src/lib/hooks/useEventTasks.ts"
      to: "/api/events/projects/[id]/tasks"
      via: "TanStack Query fetch"
      pattern: "api/events/projects"
---

<objective>
Refactor the sidebar to show Events as the primary navigation item with Calendar and Planning nested under it (including a Dashboard link for Plan 06), and create all TanStack Query hooks for event project, schedule, and task data.

Purpose: The sidebar refactor is the most visible "Events Are the Product" change. Creating all hooks here means Plans 05 and 06 can focus purely on UI components without needing to build data fetching.
Output: Refactored sidebar, 3 hook files (event project, schedule, tasks).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-event-foundation/19-RESEARCH.md
@.planning/phases/19-event-foundation/19-03-SUMMARY.md

@src/components/Sidebar.tsx
@src/lib/animations.ts

<interfaces>
<!-- API routes from Plan 03 -->

GET /api/events/projects — returns { ok: true, data: EventProject[], meta: PaginationMeta }
POST /api/events/projects — body: CreateEventProjectSchema -> { ok: true, data: EventProject }
GET /api/events/projects/[id] — returns { ok: true, data: EventProject (with scheduleBlocks, tasks, activityLog, createdBy) }
PATCH /api/events/projects/[id] — body: UpdateEventProjectSchema -> { ok: true, data: EventProject }
GET /api/events/projects/[id]/activity — returns { ok: true, data: EventActivityLog[] }
POST /api/events/projects/[id]/approve — returns { ok: true, data: EventProject }
GET /api/events/projects/[id]/schedule — returns { ok: true, data: EventScheduleBlock[] }
POST /api/events/projects/[id]/schedule — body: CreateScheduleBlockSchema -> 201
PATCH /api/events/projects/[id]/schedule/[blockId] — partial update -> 200
DELETE /api/events/projects/[id]/schedule/[blockId] -> 200
GET /api/events/projects/[id]/tasks — returns { ok: true, data: EventTask[] }
POST /api/events/projects/[id]/tasks — body: CreateEventTaskSchema -> 201
PATCH /api/events/projects/[id]/tasks/[taskId] — partial update -> 200
DELETE /api/events/projects/[id]/tasks/[taskId] -> 200

<!-- Sidebar patterns (from codebase) -->
- athleticsOpen / handleAthleticsClick / athleticsNavContent pattern
- calendarOpen / calendarNavContent pattern
- secondaryOpen = settingsOpen || calendarOpen || athleticsOpen
- navItems array with icon, label, href
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor sidebar to nest Calendar and Planning under Events with Dashboard link</name>
  <files>src/components/Sidebar.tsx</files>
  <action>
    Refactor `src/components/Sidebar.tsx`:

    a. **Add Events state** (near existing athleticsOpen/calendarOpen):
       ```typescript
       const [eventsOpen, setEventsOpen] = useState(false)
       ```

    b. **Move Calendar out of navItems**, replace with Events:
       - Remove `{ icon: Calendar, label: 'Calendar', href: '/calendar' }` from navItems
       - Add Events as the SECOND item (after Dashboard): `{ icon: CalendarClock, label: 'Events', href: '/events' }`
       - Import `CalendarClock` from lucide-react (or use a suitable event icon)

    c. **Create handleEventsClick** following handleAthleticsClick pattern:
       ```typescript
       const handleEventsClick = () => {
         if (!eventsOpen) {
           setEventsOpen(true)
           setSettingsOpen(false)
           setAthleticsOpen(false)
           setCalendarOpen(false) // Close calendar since it's now under Events
         } else {
           setEventsOpen(false)
         }
       }
       ```

    d. **Update secondaryOpen**:
       ```typescript
       const secondaryOpen = settingsOpen || calendarOpen || athleticsOpen || eventsOpen
       ```
       IMPORTANT: Keep `calendarOpen` in the expression for backward compatibility.

    e. **Auto-open Events panel on route detection** (in the useIsomorphicLayoutEffect or useEffect that detects pathname):
       ```typescript
       if (pathname.startsWith('/events') || pathname.startsWith('/calendar') || pathname.startsWith('/planning')) {
         setEventsOpen(true)
         setSettingsOpen(false)
         setAthleticsOpen(false)
       }
       ```
       Remove or adjust the existing calendar auto-open logic that opens calendarOpen directly. Instead, navigating to /calendar should open eventsOpen (the Events panel contains the calendar link).

    f. **Create eventsNavContent** — secondary panel content for Events:
       - Header: "Events" with a CalendarClock icon
       - Section 1: Navigation links (IN THIS ORDER):
         - "Dashboard" link to /events/dashboard (highlighted when pathname starts with /events/dashboard) — THIS IS REQUIRED for Plan 06's AI dashboard
         - "Calendar" link to /calendar (highlighted when pathname starts with /calendar)
         - "Planning" link to /planning (highlighted when pathname starts with /planning)
       - Section 2: "Active Events" — show a list of active EventProjects (use TanStack Query to fetch, or show a simple link to /events). Keep this lightweight — just title + status chip for the first 5 active projects.
       - Section 3 (bottom): "Create Event" button linking to /events with a create modal trigger, or just a "+ New Event" button that navigates to /events (the create flow lives on the events page).
       - Move the existing Planning CTA card from calendarNavContent to the bottom of eventsNavContent (per research recommendation).

    g. **Update secondary panel render logic**:
       Currently: `{athleticsOpen ? athleticsNavContent : calendarOpen ? calendarNavContent : settingsNavContent}`
       Change to: `{eventsOpen ? eventsNavContent : athleticsOpen ? athleticsNavContent : calendarOpen ? calendarNavContent : settingsNavContent}`
       Apply this in BOTH desktop aside and mobile nav sections.

    h. **Update aria-label** on secondary panel:
       Add `eventsOpen ? 'Events navigation' :` before the existing ternary.

    i. **Update active state logic on nav items**:
       Currently items check `!settingsOpen && !athleticsOpen` for active highlighting.
       Add `!eventsOpen` to these checks so nav items de-highlight when Events panel is open.

    CRITICAL:
    - Do NOT remove /calendar or /planning routes — they stay. Only the sidebar nav structure changes.
    - The Dashboard link (/events/dashboard) MUST be present so Plan 06 can wire to it without modifying Sidebar.tsx.
    - Simplest approach: Events panel always shows when eventsOpen=true. Inside it, Calendar link navigates to /calendar. The calendar page itself can still trigger calendarOpen for the checkbox sidebar.
  </action>
  <verify>
    <automated>cd "/Users/mkerley/Desktop/Linfield Test" && npx tsc --noEmit --pretty 2>&1 | head -20 && grep -c "eventsOpen\|events/dashboard\|handleEventsClick" src/components/Sidebar.tsx</automated>
  </verify>
  <done>Sidebar shows Events as top-level nav item with Dashboard/Calendar/Planning in secondary panel, existing /calendar and /planning URLs still work, TypeScript compiles</done>
</task>

<task type="auto">
  <name>Task 2: Create all TanStack Query hooks for event projects, schedule, and tasks</name>
  <files>src/lib/hooks/useEventProject.ts, src/lib/hooks/useEventSchedule.ts, src/lib/hooks/useEventTasks.ts</files>
  <action>
    1. Create `src/lib/hooks/useEventProject.ts`:
       - `useEventProjects(filters?)` — TanStack Query hook, fetches `GET /api/events/projects`. Returns `{ data, isLoading, error }`. Query key: `['event-projects', filters]`.
       - `useEventProject(id)` — Fetches `GET /api/events/projects/${id}`. Returns full project with schedule, tasks, activity. Query key: `['event-project', id]`. Enabled only when id is truthy.
       - `useCreateEventProject()` — useMutation for POST /api/events/projects. Invalidates `['event-projects']` on success.
       - `useUpdateEventProject(id)` — useMutation for PATCH. Invalidates both list and detail query keys.
       - `useApproveEventProject(id)` — useMutation for POST /api/events/projects/${id}/approve. Invalidates project detail.
       - `useEventActivity(id)` — Fetches GET /api/events/projects/${id}/activity. Query key: `['event-activity', id]`.
       - Follow existing hook patterns in the codebase (check `src/lib/hooks/` for fetch helper patterns).

    2. Create `src/lib/hooks/useEventSchedule.ts`:
       - `useScheduleBlocks(eventProjectId)` — GET /api/events/projects/${id}/schedule. Query key: `['event-schedule', eventProjectId]`. Returns blocks sorted by startsAt.
       - `useCreateScheduleBlock(eventProjectId)` — POST mutation. Invalidates schedule and activity queries on success.
       - `useUpdateScheduleBlock(eventProjectId)` — PATCH mutation. Invalidates schedule and activity queries.
       - `useDeleteScheduleBlock(eventProjectId)` — DELETE mutation. Invalidates schedule and activity queries.

    3. Create `src/lib/hooks/useEventTasks.ts`:
       - `useEventTasks(eventProjectId, filters?)` — GET /api/events/projects/${id}/tasks with optional status/assigneeId query params. Query key: `['event-tasks', eventProjectId, filters]`.
       - `useCreateEventTask(eventProjectId)` — POST mutation. Invalidates tasks and activity queries.
       - `useUpdateEventTask(eventProjectId)` — PATCH mutation. Invalidates tasks and activity queries.
       - `useDeleteEventTask(eventProjectId)` — DELETE mutation. Invalidates tasks and activity queries.

    Follow existing hook patterns in the codebase (check `src/lib/hooks/` for fetch helper patterns).
  </action>
  <verify>
    <automated>cd "/Users/mkerley/Desktop/Linfield Test" && npx tsx -e "import { useEventProjects, useEventProject, useCreateEventProject } from './src/lib/hooks/useEventProject'; import { useScheduleBlocks, useCreateScheduleBlock } from './src/lib/hooks/useEventSchedule'; import { useEventTasks, useCreateEventTask } from './src/lib/hooks/useEventTasks'; console.log('All hook exports verified:', [useEventProjects, useEventProject, useCreateEventProject, useScheduleBlocks, useCreateScheduleBlock, useEventTasks, useCreateEventTask].every(fn => typeof fn === 'function'))" 2>&1 | head -5</automated>
  </verify>
  <done>All 3 hook files export correctly, all CRUD hooks target correct API endpoints, TypeScript compiles</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Events appears in sidebar as top-level item
- Hook files all import and export correctly
- Dashboard link present in Events sidebar panel (/events/dashboard)
- /calendar and /planning URLs still work
</verification>

<success_criteria>
- Sidebar shows Events as primary navigation with Dashboard+Calendar+Planning nested in secondary panel
- TanStack Query hooks for event projects, schedule blocks, and tasks all export correctly
- All existing routes preserved and working
- Dashboard link in Events sidebar panel ready for Plan 06
</success_criteria>

<output>
After completion, create `.planning/phases/19-event-foundation/19-04-SUMMARY.md`
</output>
