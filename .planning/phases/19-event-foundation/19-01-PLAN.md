---
phase: 19-event-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/lib/db/index.ts
  - src/lib/permissions.ts
  - src/lib/services/organizationRegistrationService.ts
autonomous: true
requirements: [EVNT-01, EVNT-02, EVNT-03, EVNT-04, EVNT-05, EVNT-06, EVNT-07]

must_haves:
  truths:
    - "EventProject, EventSeries, EventScheduleBlock, EventTask, EventActivityLog models exist in database"
    - "All five new models are org-scoped (auto-inject organizationId)"
    - "EventProject is soft-delete enabled"
    - "New event project permissions exist and are seeded to admin/super-admin roles"
    - "Database schema pushes successfully with no errors"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "EventProject, EventSeries, EventScheduleBlock, EventTask, EventActivityLog models with all enums"
      contains: "model EventProject"
    - path: "src/lib/db/index.ts"
      provides: "Org-scoped and soft-delete registration for new models"
      contains: "EventProject"
    - path: "src/lib/permissions.ts"
      provides: "EVENT_PROJECT_* and EVENT_SERIES_MANAGE permission constants"
      contains: "EVENT_PROJECT_CREATE"
  key_links:
    - from: "prisma/schema.prisma"
      to: "src/lib/db/index.ts"
      via: "Model names in orgScopedModels and softDeleteModels sets"
      pattern: "'EventProject'"
    - from: "src/lib/permissions.ts"
      to: "src/lib/services/organizationRegistrationService.ts"
      via: "seedOrgDefaults seeds new permissions to roles"
      pattern: "events:project"
---

<objective>
Create the Prisma schema foundation for the EventProject hub model and all child models, register them in the org-scoping/soft-delete extension, define new permissions, and seed them to default roles.

Purpose: Every subsequent plan depends on these models, permissions, and DB registrations existing. This is the data foundation for the entire phase.
Output: Updated schema.prisma with 5 new models + 6 enums, updated db/index.ts with org-scoping, updated permissions.ts with 7 new permission constants, updated seed logic.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-event-foundation/19-RESEARCH.md

@prisma/schema.prisma
@src/lib/db/index.ts
@src/lib/permissions.ts
@src/lib/services/organizationRegistrationService.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add EventProject hub model and all child models to Prisma schema</name>
  <files>prisma/schema.prisma</files>
  <behavior>
    - EventProject model exists with all fields from research (id, organizationId, campusId, schoolId, title, description, coverImageUrl, startsAt, endsAt, isMultiDay, expectedAttendance, locationText, buildingId, areaId, roomId, status, source, sourceId, calendarId, createdById, approvedById, approvedAt, completedAt, metadata, timestamps, deletedAt)
    - EventSeries model exists with recurrence config (rrule, defaultStartTime, defaultDuration, defaultLocationText, defaultBuildingId, defaultRoomId, resourceNeeds, isActive)
    - EventScheduleBlock model exists (eventProjectId, type, title, description, startsAt, endsAt, locationText, leadId, sortOrder, metadata)
    - EventTask model exists (eventProjectId, title, description, status, priority, category, assigneeId, dueDate, completedAt, createdById)
    - EventActivityLog model exists (eventProjectId, actorId, type, metadata, createdAt) with NO updatedAt (immutable)
    - All 6 enums defined: EventProjectStatus, EventProjectSource, EventScheduleBlockType, EventTaskStatus, EventTaskPriority, EventActivityType
    - All relations wired to Organization, User (named relations), and parent models
    - Indexes match research spec (organizationId+status, organizationId+startsAt, source+sourceId, etc.)
    - EventSeries has relation to EventProject[] (projects field)
  </behavior>
  <action>
    Add to prisma/schema.prisma:

    1. Define 6 enums exactly as specified in the research Code Examples section:
       - EventProjectStatus (DRAFT, PENDING_APPROVAL, CONFIRMED, IN_PROGRESS, COMPLETED, CANCELLED)
       - EventProjectSource (PLANNING_SUBMISSION, SERIES, DIRECT_REQUEST)
       - EventScheduleBlockType (SESSION, ACTIVITY, MEAL, FREE_TIME, TRAVEL, SETUP)
       - EventTaskStatus (TODO, IN_PROGRESS, BLOCKED, DONE)
       - EventTaskPriority (LOW, NORMAL, HIGH, CRITICAL)
       - EventActivityType (CREATED, STATUS_CHANGE, TASK_CREATED, TASK_UPDATED, TASK_COMPLETED, SCHEDULE_BLOCK_ADDED, SCHEDULE_BLOCK_UPDATED, SCHEDULE_BLOCK_REMOVED, FIELD_UPDATED, APPROVAL_REQUESTED, APPROVAL_GRANTED, APPROVAL_REJECTED)

    2. Define all 5 models following the research schema exactly. Key points:
       - EventProject: `deletedAt DateTime?` for soft-delete. Relations to Organization, Campus?, School?, Building?, Area?, Room?, User (createdBy named "EventProjectCreator"), User? (approvedBy named "EventProjectApprover"). Has children: scheduleBlocks, tasks, activityLog.
       - EventSeries: `seriesId String?` on EventProject for back-reference. EventSeries has `projects EventProject[]`. Creator relation named "EventSeriesCreator".
       - EventScheduleBlock: FK to EventProject. Lead relation named "ScheduleBlockLead".
       - EventTask: FK to EventProject. Assignee named "EventTaskAssignee", creator named "EventTaskCreator".
       - EventActivityLog: FK to EventProject. Actor named "EventActivityActor". NO updatedAt field.

    3. Add relation fields on the Organization model: `eventProjects EventProject[]`, `eventSeries EventSeries[]`, `eventScheduleBlocks EventScheduleBlock[]`, `eventTasks EventTask[]`, `eventActivityLogs EventActivityLog[]`.

    4. Add relation fields on the User model (use named relations):
       - `eventProjectsCreated EventProject[] @relation("EventProjectCreator")`
       - `eventProjectsApproved EventProject[] @relation("EventProjectApprover")`
       - `eventSeriesCreated EventSeries[] @relation("EventSeriesCreator")`
       - `scheduleBlocksLed EventScheduleBlock[] @relation("ScheduleBlockLead")`
       - `eventTasksAssigned EventTask[] @relation("EventTaskAssignee")`
       - `eventTasksCreated EventTask[] @relation("EventTaskCreator")`
       - `eventActivitiesPerformed EventActivityLog[] @relation("EventActivityActor")`

    5. Add optional relations on Building, Area, Room, School, Campus models to EventProject (if not already present).

    6. Add `seriesId String?` and `series EventSeries? @relation(...)` to EventProject model.

    IMPORTANT: Follow existing schema conventions exactly — use `@id @default(cuid())`, `@relation(fields: [...], references: [id], onDelete: Cascade)` for org relation, `onDelete: Restrict` for user relations, `onDelete: SetNull` for optional user relations.
  </action>
  <verify>
    <automated>cd "/Users/mkerley/Desktop/Linfield Test" && npx prisma validate</automated>
  </verify>
  <done>All 5 models and 6 enums exist in schema.prisma, prisma validate passes, all relations and indexes are defined</done>
</task>

<task type="auto">
  <name>Task 2: Register new models in org-scope/soft-delete extension and add permissions with seed</name>
  <files>src/lib/db/index.ts, src/lib/permissions.ts, src/lib/services/organizationRegistrationService.ts</files>
  <action>
    1. In `src/lib/db/index.ts`:
       - Add to `orgScopedModels` Set (after the "// Leo memory module" comment block, add a new "// Event Project module" comment):
         'EventProject', 'EventSeries', 'EventScheduleBlock', 'EventTask', 'EventActivityLog'
       - Add to `softDeleteModels` Set:
         'EventProject'

    2. In `src/lib/permissions.ts`:
       - Add new permission constants to the `PERMISSIONS` object (after the EVENTS section, add a new "// Event Projects" section):
         ```
         EVENT_PROJECT_CREATE: 'events:project:create',
         EVENT_PROJECT_READ: 'events:project:read',
         EVENT_PROJECT_UPDATE_OWN: 'events:project:update:own',
         EVENT_PROJECT_UPDATE_ALL: 'events:project:update:all',
         EVENT_PROJECT_DELETE: 'events:project:delete',
         EVENT_PROJECT_APPROVE: 'events:project:approve',
         EVENT_SERIES_MANAGE: 'events:series:manage',
         ```
       - Add all 7 new permissions to `DEFAULT_ROLES.ADMIN.permissions` array
       - Add all 7 new permissions to `DEFAULT_ROLES.MEMBER.permissions` array (only READ, CREATE, UPDATE_OWN — NOT delete, approve, series manage)

    3. In `src/lib/services/organizationRegistrationService.ts`:
       - In the `seedOrgDefaults` function, verify that the permission upsert loop already handles new PERMISSIONS entries dynamically (it should — it iterates Object.values(PERMISSIONS)). If it does, no change needed here.
       - If the seed function hard-codes permission strings, add the 7 new permission strings to the seed list.

    4. Push schema to local database:
       ```bash
       npm run db:push
       ```
  </action>
  <verify>
    <automated>cd "/Users/mkerley/Desktop/Linfield Test" && npx prisma validate && node -e "const p = require('./src/lib/permissions.ts'); console.log('Missing:', ['EVENT_PROJECT_CREATE','EVENT_PROJECT_READ','EVENT_PROJECT_APPROVE','EVENT_SERIES_MANAGE'].filter(k => !p.PERMISSIONS[k]))" 2>/dev/null || npx tsx -e "import { PERMISSIONS } from './src/lib/permissions'; const needed = ['EVENT_PROJECT_CREATE','EVENT_PROJECT_READ','EVENT_PROJECT_APPROVE','EVENT_SERIES_MANAGE']; const missing = needed.filter(k => !(PERMISSIONS as any)[k]); if (missing.length) { console.error('Missing:', missing); process.exit(1); } else { console.log('All permissions present'); }"</automated>
  </verify>
  <done>All 5 new models registered in orgScopedModels, EventProject in softDeleteModels, 7 new permissions in PERMISSIONS constant, admin/member roles updated, db:push succeeds</done>
</task>

</tasks>

<verification>
- `npx prisma validate` passes
- `npm run db:push` completes without error (local DB)
- grep confirms all 5 models in orgScopedModels: `grep -c "EventProject\|EventSeries\|EventScheduleBlock\|EventTask\|EventActivityLog" src/lib/db/index.ts` returns 5+
- grep confirms permissions: `grep -c "events:project\|events:series" src/lib/permissions.ts` returns 7+
</verification>

<success_criteria>
- Prisma schema has EventProject, EventSeries, EventScheduleBlock, EventTask, EventActivityLog models
- All 6 enums defined (EventProjectStatus, EventProjectSource, EventScheduleBlockType, EventTaskStatus, EventTaskPriority, EventActivityType)
- db/index.ts registers all 5 models as org-scoped, EventProject as soft-delete
- permissions.ts has 7 new EVENT_PROJECT_* and EVENT_SERIES_MANAGE constants
- Default admin role includes all 7 new permissions
- Local database updated via db:push
</success_criteria>

<output>
After completion, create `.planning/phases/19-event-foundation/19-01-SUMMARY.md`
</output>
