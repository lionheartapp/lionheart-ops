---
phase: 23-schema-permissions-and-rls-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/permissions.ts
autonomous: true
requirements:
  - SCHEMA-03

must_haves:
  truths:
    - "6 messaging permission strings exist as PERMISSIONS constants"
    - "All non-viewer, non-board, non-parent, non-student-technician roles receive channels:create and dms:send"
    - "Admin, head-of-schools, and principal roles receive full messaging moderation permissions"
    - "Viewer, board-member, parent, and student-technician roles get no messaging permissions"
    - "integrations:manage is reused from PERMISSIONS.INTEGRATIONS_MANAGE, not duplicated"
  artifacts:
    - path: "src/lib/permissions.ts"
      provides: "5 new MESSAGING_* constants in PERMISSIONS object + role mappings in DEFAULT_ROLES"
      contains: "MESSAGING_CHANNELS_CREATE"
  key_links:
    - from: "src/lib/permissions.ts DEFAULT_ROLES"
      to: "src/lib/services/organizationRegistrationService.ts syncRolePermissions()"
      via: "syncRolePermissions reads DEFAULT_ROLES and upserts RolePermission rows"
      pattern: "PERMISSIONS.MESSAGING_"
---

<objective>
Add 5 new messaging permission constants to the PERMISSIONS object and map them to all roles in DEFAULT_ROLES per the permission matrix (D-01, D-02).

Purpose: Messaging permissions must exist before any API routes or UI gating can reference them.
Output: Updated src/lib/permissions.ts with messaging constants and role mappings.
</objective>

<execution_context>
@/Users/mkerley/Desktop/Linfield Test/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mkerley/Desktop/Linfield Test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-schema-permissions-and-rls-foundation/23-CONTEXT.md
@.planning/phases/23-schema-permissions-and-rls-foundation/23-PATTERNS.md

<interfaces>
<!-- From src/lib/permissions.ts — existing PERMISSIONS object ends at line 262 -->
<!-- INTEGRATIONS_MANAGE already exists at line 254 as 'integrations:manage' -->
<!-- ALL: '*:*' is the last entry (line 261) — insert new constants BEFORE it -->

<!-- DEFAULT_ROLES structure (lines 271-1027): -->
<!-- SUPER_ADMIN: has PERMISSIONS.ALL — no changes needed -->
<!-- ADMIN: permissions array (lines 283-455) -->
<!-- HEAD_OF_SCHOOLS: permissions array -->
<!-- PRINCIPAL: permissions array -->
<!-- MEMBER: permissions array -->
<!-- TEACHER: permissions array -->
<!-- VIEWER: permissions array — NO messaging permissions -->
<!-- ATHLETIC_DIRECTOR: permissions array -->
<!-- COACH: permissions array -->
<!-- MAINTENANCE_HEAD: permissions array -->
<!-- MAINTENANCE_TECHNICIAN: permissions array -->
<!-- IT_COORDINATOR: permissions array -->
<!-- STUDENT_TECHNICIAN: permissions array — NO messaging permissions -->
<!-- SECRETARY: permissions array -->
<!-- BOARD_MEMBER: permissions array — NO messaging permissions -->
<!-- PARENT: permissions array — NO messaging permissions -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add messaging permission constants to PERMISSIONS object</name>
  <files>src/lib/permissions.ts</files>
  <read_first>
    - src/lib/permissions.ts (full file — see existing PERMISSIONS entries, line numbers for insertion point, and INTEGRATIONS_MANAGE at line 254)
    - .planning/phases/23-schema-permissions-and-rls-foundation/23-CONTEXT.md (D-01: exact 6 permission strings)
    - .planning/phases/23-schema-permissions-and-rls-foundation/23-PATTERNS.md (permission constants pattern and collision note)
  </read_first>
  <action>
Insert 5 new constants into the PERMISSIONS object, AFTER the `FORMS_MANAGE` entry and BEFORE the `ALL: '*:*'` wildcard (per D-01). Do NOT add a 6th constant for `integrations:manage` — that already exists as `PERMISSIONS.INTEGRATIONS_MANAGE` on line 254.

```typescript
// Messaging — Phase 23
MESSAGING_CHANNELS_CREATE:     'channels:create',
MESSAGING_CHANNELS_MANAGE:     'channels:manage',
MESSAGING_CHANNELS_MODERATE:   'channels:moderate',
MESSAGING_MESSAGES_DELETE_ANY: 'messages:delete:any',
MESSAGING_DMS_SEND:            'dms:send',
// Note: integrations:manage for messaging reuses PERMISSIONS.INTEGRATIONS_MANAGE (line 254)
```

That is exactly 5 new constants. Combined with the existing INTEGRATIONS_MANAGE, this fulfills all 6 permission strings from D-01.
  </action>
  <verify>
    <automated>cd /Users/mkerley/Desktop/Linfield Test && grep -c "MESSAGING_" src/lib/permissions.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/permissions.ts contains `MESSAGING_CHANNELS_CREATE: 'channels:create'`
    - src/lib/permissions.ts contains `MESSAGING_CHANNELS_MANAGE: 'channels:manage'`
    - src/lib/permissions.ts contains `MESSAGING_CHANNELS_MODERATE: 'channels:moderate'`
    - src/lib/permissions.ts contains `MESSAGING_MESSAGES_DELETE_ANY: 'messages:delete:any'`
    - src/lib/permissions.ts contains `MESSAGING_DMS_SEND: 'dms:send'`
    - src/lib/permissions.ts does NOT contain a second `'integrations:manage'` string (grep returns exactly 1 match)
    - grep -c "MESSAGING_" returns 5 (the 5 new constant definitions)
  </acceptance_criteria>
  <done>5 new MESSAGING_* permission constants exist in the PERMISSIONS object. integrations:manage is reused, not duplicated.</done>
</task>

<task type="auto">
  <name>Task 2: Map messaging permissions to all roles in DEFAULT_ROLES per D-02</name>
  <files>src/lib/permissions.ts</files>
  <read_first>
    - src/lib/permissions.ts (the full DEFAULT_ROLES object — see each role's permissions array)
    - .planning/phases/23-schema-permissions-and-rls-foundation/23-CONTEXT.md (D-02: open by default, viewers read-only)
    - .planning/phases/23-schema-permissions-and-rls-foundation/23-PATTERNS.md (permission matrix section — exact mapping per role)
  </read_first>
  <action>
Append messaging permissions to the `permissions` array of each role in DEFAULT_ROLES. Add a `// Phase 23: Messaging` comment before each block.

**SUPER_ADMIN** — No changes. Has `PERMISSIONS.ALL` which covers everything.

**ADMIN** — Append to end of permissions array (check if INTEGRATIONS_MANAGE is already present — it likely is from Phase 22; do NOT add it again):
```typescript
// Phase 23: Messaging
PERMISSIONS.MESSAGING_CHANNELS_CREATE,
PERMISSIONS.MESSAGING_CHANNELS_MANAGE,
PERMISSIONS.MESSAGING_CHANNELS_MODERATE,
PERMISSIONS.MESSAGING_MESSAGES_DELETE_ANY,
PERMISSIONS.MESSAGING_DMS_SEND,
```

**HEAD_OF_SCHOOLS** — Append:
```typescript
// Phase 23: Messaging
PERMISSIONS.MESSAGING_CHANNELS_CREATE,
PERMISSIONS.MESSAGING_CHANNELS_MANAGE,
PERMISSIONS.MESSAGING_CHANNELS_MODERATE,
PERMISSIONS.MESSAGING_MESSAGES_DELETE_ANY,
PERMISSIONS.MESSAGING_DMS_SEND,
```

**PRINCIPAL** — Append:
```typescript
// Phase 23: Messaging
PERMISSIONS.MESSAGING_CHANNELS_CREATE,
PERMISSIONS.MESSAGING_CHANNELS_MANAGE,
PERMISSIONS.MESSAGING_CHANNELS_MODERATE,
PERMISSIONS.MESSAGING_MESSAGES_DELETE_ANY,
PERMISSIONS.MESSAGING_DMS_SEND,
```

**MEMBER** — Append (create + DMs only, no moderation):
```typescript
// Phase 23: Messaging
PERMISSIONS.MESSAGING_CHANNELS_CREATE,
PERMISSIONS.MESSAGING_DMS_SEND,
```

**TEACHER** — Append:
```typescript
// Phase 23: Messaging
PERMISSIONS.MESSAGING_CHANNELS_CREATE,
PERMISSIONS.MESSAGING_DMS_SEND,
```

**VIEWER** — NO messaging permissions added. Per D-02, viewers are read-only.

**ATHLETIC_DIRECTOR** — Append:
```typescript
// Phase 23: Messaging
PERMISSIONS.MESSAGING_CHANNELS_CREATE,
PERMISSIONS.MESSAGING_DMS_SEND,
```

**COACH** — Append:
```typescript
// Phase 23: Messaging
PERMISSIONS.MESSAGING_CHANNELS_CREATE,
PERMISSIONS.MESSAGING_DMS_SEND,
```

**MAINTENANCE_HEAD** — Append:
```typescript
// Phase 23: Messaging
PERMISSIONS.MESSAGING_CHANNELS_CREATE,
PERMISSIONS.MESSAGING_DMS_SEND,
```

**MAINTENANCE_TECHNICIAN** — Append:
```typescript
// Phase 23: Messaging
PERMISSIONS.MESSAGING_CHANNELS_CREATE,
PERMISSIONS.MESSAGING_DMS_SEND,
```

**IT_COORDINATOR** — Append:
```typescript
// Phase 23: Messaging
PERMISSIONS.MESSAGING_CHANNELS_CREATE,
PERMISSIONS.MESSAGING_DMS_SEND,
```

**STUDENT_TECHNICIAN** — NO messaging permissions. Limited role.

**SECRETARY** — Append:
```typescript
// Phase 23: Messaging
PERMISSIONS.MESSAGING_CHANNELS_CREATE,
PERMISSIONS.MESSAGING_DMS_SEND,
```

**BOARD_MEMBER** — NO messaging permissions. External read-only role.

**PARENT** — NO messaging permissions. Interacts via portal, not messaging.
  </action>
  <verify>
    <automated>cd /Users/mkerley/Desktop/Linfield Test && grep -c "MESSAGING_CHANNELS_CREATE" src/lib/permissions.ts && grep -c "MESSAGING_DMS_SEND" src/lib/permissions.ts && npx tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - grep "MESSAGING_CHANNELS_CREATE" returns 12 matches (1 definition + 11 role usages: admin, head-of-schools, principal, member, teacher, athletic-director, coach, maintenance-head, maintenance-technician, it-coordinator, secretary)
    - grep "MESSAGING_DMS_SEND" returns 12 matches (same pattern)
    - grep "MESSAGING_CHANNELS_MODERATE" returns 4 matches (1 definition + admin, head-of-schools, principal)
    - grep "MESSAGING_MESSAGES_DELETE_ANY" returns 4 matches (1 definition + admin, head-of-schools, principal)
    - The VIEWER role section does NOT contain any MESSAGING_ string
    - The BOARD_MEMBER role section does NOT contain any MESSAGING_ string
    - The STUDENT_TECHNICIAN role section does NOT contain any MESSAGING_ string
    - The PARENT role section does NOT contain any MESSAGING_ string
    - `npx tsc --noEmit` passes (no type errors)
  </acceptance_criteria>
  <done>All roles have correct messaging permission mappings per D-02. Operational staff get create+DMs, admin-tier gets full moderation, restricted roles get nothing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Permission constants -> role seed | Incorrect mapping means wrong users get messaging access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03 | Elevation of Privilege | DEFAULT_ROLES permission mapping | mitigate | Viewer, board-member, parent, student-technician excluded from messaging permissions per D-02; verified by grep count |
| T-23-04 | Elevation of Privilege | integrations:manage duplication | mitigate | Reuse PERMISSIONS.INTEGRATIONS_MANAGE — do not create a second constant with the same string value |
</threat_model>

<verification>
- 5 new MESSAGING_* constants exist in PERMISSIONS
- grep counts match expected role distribution
- No duplicate integrations:manage string
- TypeScript compiles without errors
</verification>

<success_criteria>
- PERMISSIONS object has all 5 messaging constants with correct string values
- Every role in DEFAULT_ROLES has the correct messaging permissions per the D-02 matrix
- Restricted roles (viewer, board-member, parent, student-technician) have zero messaging permissions
- No type errors
</success_criteria>

<output>
After completion, create `.planning/phases/23-schema-permissions-and-rls-foundation/23-02-SUMMARY.md`
</output>
