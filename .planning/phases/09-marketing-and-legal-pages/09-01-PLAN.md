---
phase: 09-marketing-and-legal-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/privacy/page.tsx
  - src/app/terms/page.tsx
  - src/components/public/PublicNav.tsx
  - src/components/public/PublicFooter.tsx
autonomous: true
requirements:
  - PAGE-01
  - PAGE-02

must_haves:
  truths:
    - "Visitor can read a Privacy Policy page with explicit COPPA and FERPA sections"
    - "Visitor can read a Terms of Service page with all standard SaaS legal sections"
    - "Both pages share consistent navigation and footer with the landing page"
  artifacts:
    - path: "src/app/privacy/page.tsx"
      provides: "Privacy Policy page with COPPA/FERPA compliance"
      contains: "COPPA Compliance"
    - path: "src/app/terms/page.tsx"
      provides: "Terms of Service page"
      contains: "Acceptance of Terms"
    - path: "src/components/public/PublicNav.tsx"
      provides: "Shared public navigation component"
      exports: ["PublicNav"]
    - path: "src/components/public/PublicFooter.tsx"
      provides: "Shared public footer component"
      exports: ["PublicFooter"]
  key_links:
    - from: "src/app/privacy/page.tsx"
      to: "src/components/public/PublicNav.tsx"
      via: "import"
      pattern: "import.*PublicNav"
    - from: "src/app/terms/page.tsx"
      to: "src/components/public/PublicFooter.tsx"
      via: "import"
      pattern: "import.*PublicFooter"
---

<objective>
Create the Privacy Policy and Terms of Service pages, plus shared public navigation and footer components extracted from the landing page.

Purpose: PAGE-01 requires a Privacy Policy that explicitly addresses COPPA and FERPA for K-12 schools. PAGE-02 requires a Terms of Service governing platform usage. Both pages need consistent nav/footer, so we extract reusable PublicNav and PublicFooter components that all public pages (Plans 02 and 03) will import.

Output: Two legal content pages at `/privacy` and `/terms`, plus `PublicNav.tsx` and `PublicFooter.tsx` shared components.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-marketing-and-legal-pages/09-RESEARCH.md

@src/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract PublicNav and PublicFooter shared components</name>
  <files>src/components/public/PublicNav.tsx, src/components/public/PublicFooter.tsx</files>
  <action>
Extract the navigation bar and footer from `src/app/page.tsx` into two reusable client components.

**PublicNav.tsx** (`'use client'`):
- Extract the `<nav>` element from the landing page (lines 55-77 of page.tsx)
- Same markup: logo on left, Sign In (ghost) + Get Started (accent) buttons on right
- Use `Link` from `next/link` for all internal links (not `<a>` tags)
- Logo links to `/` via `Link`
- Keep the same Tailwind classes, responsive behavior, and focus-visible styles
- Accept no props — it is self-contained

**PublicFooter.tsx** (`'use client'`):
- Extract the `<footer>` element from the landing page (lines 301-334 of page.tsx)
- Convert all `<a href="#">` links to `Link` from `next/link` with real paths:
  - Product column: Features -> `/#features`, Pricing -> `/pricing`
  - Company column: About -> `/about`, Contact -> `/about#contact`
  - Legal column: Privacy -> `/privacy`, Terms -> `/terms`
- Keep the same dark bg-gray-900 styling, 4-column grid, copyright line
- Use `Link` for internal pages, but Features can use a regular `<a href="/#features">` since it's an anchor link on another page (or use Link with href="/#features")
- Accept no props

**Do NOT modify `src/app/page.tsx` in this task.** The landing page keeps its inline nav/footer for now — changing it is out of scope (it would require careful testing of the landing page). The new pages will use the extracted components.
  </action>
  <verify>
    <automated>npx tsc --noEmit --strict src/components/public/PublicNav.tsx src/components/public/PublicFooter.tsx 2>&1 || echo "Check TS errors above"</automated>
  </verify>
  <done>PublicNav.tsx and PublicFooter.tsx exist, export default components, compile without errors, and use Link from next/link for all internal routes.</done>
</task>

<task type="auto">
  <name>Task 2: Create Privacy Policy and Terms of Service pages</name>
  <files>src/app/privacy/page.tsx, src/app/terms/page.tsx</files>
  <action>
Create two Server Components (no `'use client'` directive needed — pure content pages).

**Privacy Policy page** (`src/app/privacy/page.tsx`):
- Export `generateMetadata` with title "Privacy Policy | Lionheart" and description mentioning COPPA/FERPA compliance for K-12 schools
- Import and render `PublicNav` at top and `PublicFooter` at bottom
- Main content area: `max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 py-16`
- Use Tailwind prose classes for typography: `prose prose-gray prose-headings:font-semibold`
- Include "Last updated: March 2026" at top
- **Required sections** (all must be present):
  1. What We Collect — Organization data, user data, operational data, technical data
  2. How We Use Data — Service delivery, internal operations, communications, security
  3. Data Sharing — Third-party processors (Supabase, Resend, Gemini), no sale of data
  4. **FERPA Compliance** (must have this exact heading) — Lionheart acts as a "school official" with legitimate educational interest; accesses education records only as directed by the school; schools retain FERPA-compliant ownership of student data; no disclosure to third parties without school consent
  5. **COPPA Compliance** (must have this exact heading) — Platform is for school administration, not direct children's use; any student data entered by school staff is controlled by the school under FERPA; schools responsible for obtaining necessary parental consent
  6. Data Retention — Active account data retained for account lifetime; deleted on cancellation
  7. Security — Encryption at rest/transit, httpOnly cookies, role-based access controls
  8. Your Rights — Access, correction, deletion requests; contact method
  9. Contact — privacy@lionheartapp.com as contact email
- Each section uses `<h2>` tags with clear section numbers or headings
- Content should be substantive paragraphs (not single-line placeholders) — aim for 1500+ words total

**Terms of Service page** (`src/app/terms/page.tsx`):
- Export `generateMetadata` with title "Terms of Service | Lionheart"
- Same layout pattern: PublicNav + content + PublicFooter
- Include "Last updated: March 2026" at top
- **Required sections:**
  1. Acceptance of Terms
  2. Description of Service — School operations management SaaS, multi-tenant
  3. Account Registration — Organization admin creates account; responsible for all users
  4. Acceptable Use — No illegal activity, no unauthorized access, no harmful content
  5. Data Ownership — Customer owns their data; Lionheart is processor
  6. Payment Terms — Subscription billing; free trial terms
  7. Service Availability — Best-effort uptime; maintenance windows
  8. Limitation of Liability — Cap at fees paid in prior 12 months
  9. Termination — Either party may terminate; data export on request
  10. Governing Law — Jurisdiction (use generic "State of incorporation")
  11. Changes to Terms — 30-day notice for material changes
  12. Contact — legal@lionheartapp.com
- Substantive content for each section — aim for 1200+ words total

**Styling notes for both pages:**
- Match the landing page's clean, white background aesthetic
- Use `min-h-screen bg-white` wrapper
- No animations needed on legal pages — these are content-focused
- Ensure good heading hierarchy: h1 for page title, h2 for sections
  </action>
  <verify>
    <automated>curl -s http://localhost:3004/privacy 2>/dev/null | grep -c "COPPA\|FERPA" || echo "Start dev server first: npm run dev"</automated>
  </verify>
  <done>
- `/privacy` renders with all 9 sections including explicit "COPPA Compliance" and "FERPA Compliance" headings
- `/terms` renders with all 11 sections
- Both pages use PublicNav and PublicFooter components
- Both export generateMetadata for SEO
  </done>
</task>

</tasks>

<verification>
1. `curl -s http://localhost:3004/privacy | grep -i "coppa"` returns matches (PAGE-01 COPPA requirement)
2. `curl -s http://localhost:3004/privacy | grep -i "ferpa"` returns matches (PAGE-01 FERPA requirement)
3. `curl -s http://localhost:3004/terms | grep -i "acceptance of terms"` returns matches (PAGE-02)
4. Both pages return 200 status codes
5. TypeScript compiles without errors: `npx tsc --noEmit`
</verification>

<success_criteria>
- Privacy Policy page at `/privacy` loads with COPPA and FERPA sections explicitly named (PAGE-01)
- Terms of Service page at `/terms` loads with complete legal content (PAGE-02)
- PublicNav and PublicFooter components exist and are reusable by Plans 02 and 03
</success_criteria>

<output>
After completion, create `.planning/phases/09-marketing-and-legal-pages/09-01-SUMMARY.md`
</output>
