---
phase: 09-marketing-and-legal-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/pricing/page.tsx
autonomous: true
requirements:
  - PAGE-03

must_haves:
  truths:
    - "Visitor can view a Pricing page showing three plan tiers before signup"
    - "Visitor can toggle between monthly and annual billing to see discounted prices"
    - "The Professional tier is visually recommended as the best value"
  artifacts:
    - path: "src/app/pricing/page.tsx"
      provides: "Pricing page with three-tier comparison"
      contains: "Professional"
  key_links:
    - from: "src/app/pricing/page.tsx"
      to: "/signup"
      via: "Link href"
      pattern: "href.*signup"
---

<objective>
Create the Pricing page with a three-tier plan comparison, monthly/annual toggle, feature comparison, and FAQ section.

Purpose: PAGE-03 requires visitors to see pricing before signing up. The Pricing page is the primary conversion page for prospects evaluating Lionheart.

Output: A polished pricing page at `/pricing` with Starter/Professional/Enterprise tiers, billing toggle, feature table, and FAQ.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-marketing-and-legal-pages/09-RESEARCH.md

@src/app/page.tsx
@.claude/skills/pricing-strategy/SKILL.md
@.claude/skills/page-cro/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Pricing page with three-tier comparison</name>
  <files>src/app/pricing/page.tsx</files>
  <action>
Create a client component (`'use client'` needed for billing toggle state) at `src/app/pricing/page.tsx`.

**Page structure:**

1. **Navigation**: Import and use `PublicNav` from `@/components/public/PublicNav` (created in Plan 01, Wave 1). If Plan 01 hasn't run yet, create a minimal inline nav matching the landing page pattern (logo + Sign In + Get Started) — the executor should check if PublicNav exists first and prefer it.

2. **Hero section**:
   - Headline: "Simple, transparent pricing" (or similar benefit-focused headline)
   - Subheadline: "Choose the plan that fits your school. No hidden fees, cancel anytime."
   - Monthly/Annual toggle switch — annual shows ~20% savings with a "Save 20%" badge

3. **Three pricing cards** in a responsive grid (`grid-cols-1 md:grid-cols-3 gap-8`):

   **Starter** — $99/mo ($79/mo annual)
   - "For small schools getting started"
   - Up to 1 campus
   - Up to 50 staff accounts
   - IT & Maintenance ticketing
   - Knowledge base
   - Email support
   - CTA: "Start Free Trial" linking to `/signup`

   **Professional** (recommended) — $199/mo ($159/mo annual)
   - "For growing schools that need everything"
   - **"Most Popular" badge** at top of card
   - Visual emphasis: primary-colored border or gradient accent, slightly larger/elevated
   - Unlimited campuses
   - Unlimited staff accounts
   - Everything in Starter, plus:
   - Athletics module
   - Calendar & events
   - AI-powered diagnostics
   - Compliance & board reporting
   - Preventive maintenance scheduling
   - Priority support
   - CTA: "Start Free Trial" linking to `/signup` (primary/accent button style)

   **Enterprise** — "Custom" pricing
   - "For large districts with custom needs"
   - Everything in Professional, plus:
   - Dedicated onboarding
   - Custom integrations
   - SLA guarantee
   - SSO/SAML (when available)
   - Dedicated account manager
   - CTA: "Contact Sales" linking to `/about#contact`

4. **Feature comparison table** below the cards:
   - Responsive: on mobile, show a simplified list; on desktop, show a full comparison grid
   - Categories: Core Features, Modules, Support, Limits
   - Checkmarks for included features, dashes for excluded
   - Use lucide-react `Check` icon for included

5. **FAQ section** (3-5 questions):
   - "Can I change plans later?" — Yes, upgrade or downgrade anytime
   - "Is there a free trial?" — Yes, 14-day free trial on all plans, no credit card required
   - "What happens when my trial ends?" — You choose a plan or your account is paused
   - "Do you offer discounts for schools?" — Annual billing saves 20%; contact us for multi-school district pricing
   - "How do I cancel?" — Cancel anytime from your Settings, no cancellation fees

6. **Footer**: Import and use `PublicFooter` from `@/components/public/PublicFooter` (same note as nav — check existence first, fallback to inline).

**Styling:**
- Use Framer Motion for card entrance animations (stagger), matching the landing page's animation style
- Import animation variants from `@/lib/animations` where they exist, or define local ones matching the `[0.25, 0.1, 0.25, 1]` easing
- Cards: `bg-white border border-gray-200 rounded-2xl p-8 shadow-sm` for normal, gradient accent or `border-primary-500 shadow-lg` for Professional
- Toggle: pill-style switch with smooth transition
- Trust signals near CTAs: "No credit card required" and "Cancel anytime" text
- Match the landing page's clean white aesthetic with primary-600 accents
- Mobile responsive: cards stack on small screens

**Pricing psychology (per pricing-strategy skill):**
- Anchoring: Professional tier is center and visually emphasized
- "Most Popular" badge creates social proof
- Annual discount displayed as savings per year ("Save $480/year")
- Enterprise "Contact Sales" creates price anchor (most expensive perceived)

**SEO:**
- Export `metadata` object (not `generateMetadata` since this is a client component) — actually, wrap the page: make the page.tsx export a default Server Component that renders the client PricingContent. Or just set metadata via `<title>` and `<meta>` in `<head>` if needed. Simplest approach: use a client component and set `document.title` in useEffect, or better: create a separate `layout.tsx` in the pricing folder that exports metadata.

Best approach: Create `src/app/pricing/layout.tsx` (Server Component) that exports metadata, and `src/app/pricing/page.tsx` as the client component with the interactive toggle.

`src/app/pricing/layout.tsx`:
```typescript
export const metadata = {
  title: 'Pricing | Lionheart',
  description: 'Simple, transparent pricing for school operations management. Start free, upgrade when ready.',
}
export default function PricingLayout({ children }: { children: React.ReactNode }) {
  return children
}
```
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" http://localhost:3004/pricing 2>/dev/null || echo "Start dev server: npm run dev"</automated>
  </verify>
  <done>
- `/pricing` returns 200 and renders three plan tiers (Starter, Professional, Enterprise)
- Monthly/annual billing toggle works and updates displayed prices
- Professional tier has "Most Popular" badge and visual emphasis
- Feature comparison table shows included/excluded features per tier
- FAQ section with 4-5 expandable questions
- All CTA buttons link to /signup or /about#contact
  </done>
</task>

</tasks>

<verification>
1. `curl -s http://localhost:3004/pricing` returns 200 and contains "Starter", "Professional", "Enterprise"
2. Page includes "Most Popular" text for Professional tier
3. All CTA links point to `/signup` or `/about#contact` (no dead links)
4. TypeScript compiles: `npx tsc --noEmit`
</verification>

<success_criteria>
- Pricing page at `/pricing` shows three tiers with prices visible without signup (PAGE-03)
- Monthly/annual toggle works
- Professional tier visually highlighted as recommended
- CTA buttons link to signup flow
</success_criteria>

<output>
After completion, create `.planning/phases/09-marketing-and-legal-pages/09-02-SUMMARY.md`
</output>
