---
phase: 22-ai-budget-notifications-and-external-integrations
plan: 06
type: execute
wave: 2
depends_on: ["22-03"]
files_modified:
  - src/components/events/ai/AIEventWizard.tsx
  - src/components/events/ai/AIEventChat.tsx
  - src/components/events/ai/AIEventPreview.tsx
  - src/app/events/new/ai/page.tsx
  - src/components/Sidebar.tsx
autonomous: true
requirements:
  - AI-01
  - AI-05

must_haves:
  truths:
    - "Staff can describe an event in natural language in a chat-style interface"
    - "AI fills all event fields and presents a pre-filled EventProject preview"
    - "Staff can edit AI suggestions before confirming creation"
    - "AI generates a multi-day schedule from the description"
    - "Chat supports conversational follow-ups to refine the event"
  artifacts:
    - path: "src/components/events/ai/AIEventWizard.tsx"
      provides: "Two-panel wizard: chat on left, preview on right"
      min_lines: 80
    - path: "src/components/events/ai/AIEventChat.tsx"
      provides: "Chat-style interface for natural language event description"
      min_lines: 60
    - path: "src/components/events/ai/AIEventPreview.tsx"
      provides: "Pre-filled event project preview with editable fields"
      min_lines: 80
    - path: "src/app/events/new/ai/page.tsx"
      provides: "Page route for AI event creation"
      min_lines: 20
  key_links:
    - from: "src/components/events/ai/AIEventChat.tsx"
      to: "/api/events/ai/create-from-description"
      via: "fetch call on message submit"
      pattern: "create-from-description"
    - from: "src/components/events/ai/AIEventPreview.tsx"
      to: "/api/events/projects"
      via: "POST to create EventProject from AI suggestions"
      pattern: "events/projects"
---

<objective>
AI event creation wizard: Chat-style interface where staff describe an event in natural language and AI auto-fills all event details, suggests documents, generates a schedule, and estimates a budget range. Staff review and edit before confirming.

Purpose: Provides the fourth event creation path (alongside planning season, series, direct request). Feels like talking to an assistant, not filling out a form (per user decision).
Output: AIEventWizard (2-panel), AIEventChat, AIEventPreview, page route, sidebar link
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-ai-budget-notifications-and-external-integrations/22-03-SUMMARY.md
@src/lib/types/event-ai.ts
@src/lib/services/ai/eventAIService.ts
@src/components/Sidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: AI chat and preview components</name>
  <files>src/components/events/ai/AIEventChat.tsx, src/components/events/ai/AIEventPreview.tsx</files>
  <action>
**Create src/components/events/ai/AIEventChat.tsx:**
- Chat-style interface with message bubbles
- Props: { onSuggestionGenerated: (suggestion: AIEventSuggestion) => void, onRefinement: (field: string, instruction: string) => void, isGenerating: boolean }
- State: messages array of { role: 'user' | 'assistant', content: string, timestamp: Date }
- Initial assistant message: "Tell me about the event you're planning. Include details like the type of event, dates, location, expected number of attendees, and any special requirements."
- Text input at bottom with send button (dark pill style per button standard)
- On first user message: calls POST /api/events/ai/create-from-description with the description. Shows typing indicator (3 bouncing dots). On response, adds assistant message summarizing what was generated and calls onSuggestionGenerated.
- Follow-up messages: user can say things like "Make it 3 days instead of 2" or "Add a pool party on day 2". These refine the existing suggestion (calls the same endpoint with accumulated context). Each refinement triggers onRefinement callback to update preview.
- Messages scroll to bottom on new message (useRef + scrollIntoView)
- User messages: right-aligned, dark bg, white text (rounded-2xl)
- Assistant messages: left-aligned, ui-glass bg, gray-900 text (rounded-2xl)
- Sparkle icon next to assistant name badge
- Typing indicator: 3 dots with bounce animation (CSS keyframes)
- If AI returns null (no API key): show error message "AI features require a Gemini API key. Please configure GEMINI_API_KEY in your environment."

**Create src/components/events/ai/AIEventPreview.tsx:**
- Props: { suggestion: AIEventSuggestion | null, onEdit: (field: string, value: any) => void, onConfirm: (data: AIEventSuggestion) => void, isCreating: boolean }
- Shows pre-filled event project preview that updates as AI generates/refines
- Sections (all editable inline):
  1. **Event Details** — Title (editable text input), Description (editable textarea), Dates (date pickers for start/end), Location (editable text), Expected Attendance (number input)
  2. **Suggested Schedule** — Table of schedule blocks with Day, Time, Title, Type columns. Each row editable. "+ Add Block" button. Delete (X) button per row. If no schedule generated, shows "Generate Schedule" button.
  3. **Suggested Documents** — Checklist of document types (permission slip, medical release, etc.) with checkboxes to include/exclude. "+ Add Document" button.
  4. **Suggested Tasks** — List of tasks with title, priority, category. Checkboxes to include/exclude. "+ Add Task" button.
  5. **Budget Estimate** — Table of categories with min-max range. Staff can adjust amounts. Shows total range at bottom.
- "Create Event" button (aurora gradient bg, white text, rounded-full) at bottom — calls onConfirm. Shows loading state during creation.
- Empty state (no suggestion yet): "Describe your event in the chat to get started" with sparkle illustration
- All sections use ui-glass cards
- Changes are local state until "Create Event" is clicked
  </action>
  <verify>
    <automated>cd "/Users/mkerley/Desktop/Linfield Test" && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Chat component handles message flow with typing indicator, preview shows editable AI suggestions with all 5 sections, both components compile</done>
</task>

<task type="auto">
  <name>Task 2: AI wizard page, orchestration, and sidebar link</name>
  <files>src/components/events/ai/AIEventWizard.tsx, src/app/events/new/ai/page.tsx, src/components/Sidebar.tsx</files>
  <action>
**Create src/components/events/ai/AIEventWizard.tsx:**
- Two-panel layout: chat on left (40% width), preview on right (60% width)
- On mobile (< lg breakpoint): stacked layout with chat on top, preview below with a toggle button to switch
- State management:
  - suggestion: AIEventSuggestion | null — populated by AI response
  - isGenerating: boolean — loading state during AI call
  - isCreating: boolean — loading state during EventProject creation
- When onSuggestionGenerated fires: update suggestion state, preview auto-populates
- When onRefinement fires: merge refinement into existing suggestion
- When onConfirm fires:
  1. POST /api/events/projects to create the EventProject with the suggestion data (title, description, dates, location, attendance, etc.)
  2. If schedule blocks exist: POST each to /api/events/projects/{id}/schedule
  3. If tasks exist: POST each to /api/events/projects/{id}/tasks
  4. If budget categories exist: POST category initialization + line items to /api/events/projects/{id}/budget
  5. Navigate to /events/{newProjectId}?tab=overview on success
- Error handling: toast on failure, don't navigate
- Header: "Create Event with AI" with sparkle icon, back arrow to /events

**Create src/app/events/new/ai/page.tsx:**
- Page component with DashboardLayout wrapper
- Auth guard (same pattern as other event pages)
- Permission check: assertCan(EVENTS_CREATE)
- Renders AIEventWizard
- Page title: "Create Event with AI"

**Modify src/components/Sidebar.tsx:**
- In the Events nav section, add "Create with AI" link alongside existing event creation paths
- Icon: Sparkles from lucide-react
- Link: /events/new/ai
- Show only to users with EVENTS_CREATE permission
- Position: below existing "New Event" or equivalent creation links in the events panel
- Add subtle aurora gradient dot indicator next to "AI" label to signal the feature
  </action>
  <verify>
    <automated>cd "/Users/mkerley/Desktop/Linfield Test" && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>AI wizard page loads with two-panel layout, creation flow creates EventProject + schedule + tasks + budget, sidebar shows "Create with AI" link</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- /events/new/ai page loads with chat + preview layout
- Chat accepts natural language and shows AI response
- Preview shows editable fields populated from AI
- "Create Event" button creates full EventProject with all suggested data
- Sidebar shows "Create with AI" link in Events section
</verification>

<success_criteria>
- Chat-style wizard feels like talking to an assistant (per user decision)
- AI fills title, dates, docs, tasks, schedule, and budget from natural language
- Staff can edit all AI suggestions before confirming
- Created event includes schedule blocks, tasks, and budget categories from AI
- Conversational follow-ups refine the existing suggestion
</success_criteria>

<output>
After completion, create `.planning/phases/22-ai-budget-notifications-and-external-integrations/22-06-SUMMARY.md`
</output>
