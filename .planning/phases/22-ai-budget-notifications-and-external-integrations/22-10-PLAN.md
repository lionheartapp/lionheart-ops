---
phase: 22-ai-budget-notifications-and-external-integrations
plan: 10
type: execute
wave: 3
depends_on: ["22-04", "22-05", "22-06", "22-07", "22-08", "22-09"]
files_modified:
  - src/lib/services/notificationOrchestrationService.ts
  - src/lib/services/eventProjectService.ts
  - src/middleware.ts
  - scripts/smoke-phase22.mjs
autonomous: false
requirements:
  - BUD-01
  - BUD-02
  - BUD-03
  - COM-03
  - AI-01
  - AI-02
  - AI-03
  - AI-04
  - AI-05
  - AI-06
  - AI-07
  - AI-08
  - AI-09
  - AI-10
  - AI-11
  - INT-01
  - INT-02
  - INT-03

must_haves:
  truths:
    - "Notification orchestration hooks into event reschedule flow"
    - "SMS delivery wired as channel in notification dispatch"
    - "Google Calendar sync triggered on event creation/update"
    - "All Phase 22 features accessible from correct navigation paths"
    - "Smoke tests validate budget, notification, AI, and integration endpoints"
  artifacts:
    - path: "scripts/smoke-phase22.mjs"
      provides: "Integration smoke tests for all Phase 22 API endpoints"
      min_lines: 100
  key_links:
    - from: "src/lib/services/eventProjectService.ts"
      to: "src/lib/services/notificationOrchestrationService.ts"
      via: "recalculateRulesForEvent on reschedule"
      pattern: "recalculateRulesForEvent"
    - from: "src/lib/services/notificationOrchestrationService.ts"
      to: "src/lib/services/integrations/twilioService.ts"
      via: "SMS delivery channel in dispatchPendingNotifications"
      pattern: "sendSMS|sendBulkSMS"
---

<objective>
Integration wiring, cross-feature connections, smoke tests, and human verification checkpoint. Wires notification orchestration into event reschedule flow, adds SMS as a notification delivery channel, triggers Google Calendar sync on event changes, and validates the complete Phase 22 feature set.

Purpose: Ensures all Phase 22 features work together as a cohesive system. The notification timeline auto-adjusts when events are rescheduled. SMS goes out alongside email and in-app notifications. Google Calendar stays in sync.
Output: Cross-feature wiring, smoke test script, human verification
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-ai-budget-notifications-and-external-integrations/22-01-SUMMARY.md
@.planning/phases/22-ai-budget-notifications-and-external-integrations/22-02-SUMMARY.md
@.planning/phases/22-ai-budget-notifications-and-external-integrations/22-03-SUMMARY.md
@.planning/phases/22-ai-budget-notifications-and-external-integrations/22-04-SUMMARY.md
@.planning/phases/22-ai-budget-notifications-and-external-integrations/22-05-SUMMARY.md
@.planning/phases/22-ai-budget-notifications-and-external-integrations/22-06-SUMMARY.md
@.planning/phases/22-ai-budget-notifications-and-external-integrations/22-07-SUMMARY.md
@.planning/phases/22-ai-budget-notifications-and-external-integrations/22-08-SUMMARY.md
@.planning/phases/22-ai-budget-notifications-and-external-integrations/22-09-SUMMARY.md
@src/lib/services/eventProjectService.ts
@src/lib/services/notificationOrchestrationService.ts
@src/middleware.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-feature wiring and middleware updates</name>
  <files>src/lib/services/notificationOrchestrationService.ts, src/lib/services/eventProjectService.ts, src/middleware.ts</files>
  <action>
**Wire notification reschedule into eventProjectService.ts:**
- Find the function that updates EventProject dates (PATCH handler or updateEventProject)
- After dates are updated, call recalculateRulesForEvent(eventProjectId)
- Log the reschedule summary (array of { ruleId, label, oldScheduledAt, newScheduledAt }) to the event's activity log via the existing activity logging pattern
- Wrap in try/catch — reschedule calculation failure should not block the date update

**Wire SMS delivery into notificationOrchestrationService.ts:**
- In dispatchPendingNotifications, after in-app and email delivery:
  - Check if org has active Twilio integration via twilioService.isAvailable(organizationId)
  - If available AND notification rule targets an audience with phone numbers:
    - Resolve audience phone numbers (from User.phone or EventRegistration contact info)
    - Call twilioService.sendBulkSMS with the message subject + body
    - Create IntegrationSyncLog entry with action='send_sms'
  - SMS failures are non-fatal (logged, not thrown)

**Wire Google Calendar sync into event creation/update:**
- In eventProjectService.ts (or the event project PATCH route):
  - After EventProject is created or confirmed (status changes to CONFIRMED):
    - Check if createdBy user has Google Calendar integration
    - If yes: call googleCalendarService.syncEventToCalendar(userId, eventProject)
    - On event delete/cancel: call googleCalendarService.removeEventFromCalendar
  - Wrap in try/catch — Google Calendar failure should not block event operations

**Update src/middleware.ts:**
- Add to public paths: /api/integrations/google-calendar/callback (OAuth callback needs to work without auth)
- Ensure /api/cron/event-notifications follows existing cron authentication pattern (CRON_SECRET header)
- Verify /api/events/ai/* routes are NOT public (they require auth)
  </action>
  <verify>
    <automated>cd "/Users/mkerley/Desktop/Linfield Test" && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Notification reschedule wired into event update flow, SMS delivery added as notification channel, Google Calendar sync triggers on event confirm, middleware updated</done>
</task>

<task type="auto">
  <name>Task 2: Smoke tests for Phase 22 endpoints</name>
  <files>scripts/smoke-phase22.mjs</files>
  <action>
Create scripts/smoke-phase22.mjs following existing smoke test patterns (smoke-campus.mjs, smoke-registration.mjs):

**Test cases (SKIP stubs per Phase 21 pattern — real assertions deferred to manual testing):**

Budget tests:
1. GET /api/events/projects/{id}/budget — returns categories and line items (SKIP stub)
2. POST /api/events/projects/{id}/budget — creates line item (SKIP stub)
3. PATCH /api/events/projects/{id}/budget/{lineId} — updates line item (SKIP stub)
4. DELETE /api/events/projects/{id}/budget/{lineId} — deletes line item (SKIP stub)
5. GET /api/events/projects/{id}/budget/revenue — returns revenue (SKIP stub)
6. POST /api/events/projects/{id}/budget/revenue — creates revenue (SKIP stub)
7. GET /api/events/projects/{id}/budget/report — returns report with per-participant cost (SKIP stub)

Notification tests:
8. GET /api/events/projects/{id}/notifications — returns rules (SKIP stub)
9. POST /api/events/projects/{id}/notifications — creates rule (SKIP stub)
10. POST /api/events/projects/{id}/notifications/{ruleId}/approve — approval flow (SKIP stub)

AI tests:
11. POST /api/events/ai/create-from-description — AI event creation (SKIP stub)
12. POST /api/events/ai/generate-schedule — AI schedule (SKIP stub)
13. POST /api/events/ai/generate-form — AI form generation (SKIP stub)
14. POST /api/events/ai/generate-groups — AI group assignments (SKIP stub)
15. POST /api/events/ai/detect-conflicts — conflict detection (SKIP stub)
16. POST /api/events/ai/estimate-budget — budget estimation (SKIP stub)
17. POST /api/events/ai/generate-summary — status summary (SKIP stub)
18. POST /api/events/ai/analyze-feedback — feedback analysis (SKIP stub)

Template tests:
19. GET /api/events/templates — list templates (SKIP stub)
20. POST /api/events/templates — save as template (SKIP stub)
21. POST /api/events/templates/{id} — create from template (SKIP stub)

Integration tests:
22. GET /api/integrations/twilio/config — Twilio config status (SKIP stub)
23. GET /api/integrations/planning-center/auth — PCO auth URL (SKIP stub)
24. GET /api/integrations/google-calendar/auth — Google auth URL (SKIP stub)

Add npm script: "smoke:phase22": "node scripts/smoke-phase22.mjs" to package.json.

Output format: numbered test case with [SKIP] badge and description, matching existing smoke test output style.
  </action>
  <verify>
    <automated>cd "/Users/mkerley/Desktop/Linfield Test" && node scripts/smoke-phase22.mjs 2>&1 | head -30</automated>
  </verify>
  <done>Smoke test script exists with 24 test case stubs, runs without error, follows existing smoke test patterns</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of complete Phase 22 feature set</name>
  <files>N/A</files>
  <action>Human verifies all Phase 22 features are accessible and functional in the browser. No code changes — verification only.</action>
  <verify>Human confirms all 10 verification steps pass</verify>
  <done>Phase 22 approved by human reviewer</done>
  <what-built>Complete Phase 22: Budget management, notification orchestration, AI event creation/form/group/conflict/summary/feedback generation, event templates, and Planning Center/Google Calendar/Twilio integrations</what-built>
  <how-to-verify>
    1. Navigate to any EventProject and click the Budget tab — verify it shows spreadsheet-style line items grouped by categories (not the old placeholder stub)
    2. In the Comms tab, scroll to the Automated Notifications section — verify the visual timeline appears with the event date marked
    3. Try creating a notification rule — verify the drawer opens with trigger type options and an "AI Draft" button
    4. Navigate to Events → Create with AI in the sidebar — verify the chat-style wizard loads with a two-panel layout
    5. On any EventProject Overview tab, verify the "Save as Template" button is visible for confirmed/completed events
    6. In Settings, verify a new "Integrations" tab appears with Planning Center, Google Calendar, and Twilio cards
    7. On the Logistics tab, verify "AI Suggest Assignments" and "Check Conflicts" buttons are present
    8. On the Registration tab, verify "Generate with AI" button is present for form generation
    9. Run `npm run smoke:phase22` — verify all 24 test stubs execute without errors
    10. Verify `npm run dev` starts without errors on port 3004
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 22, or describe any issues to address</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm run dev` starts without errors
- `npm run smoke:phase22` runs all 24 stubs
- All cross-feature wiring in place (reschedule -> notification recalc, confirm -> Google Calendar, dispatch -> SMS)
- Middleware correctly handles public callback routes
</verification>

<success_criteria>
- Budget tab functional with spreadsheet-style UI
- Notification timeline visible in Comms tab with visual pins
- AI event creation wizard accessible from sidebar
- Template save/create flow works end-to-end
- Integrations settings tab shows all three providers
- All features accessible from correct navigation paths
- Dev server runs cleanly on port 3004
</success_criteria>

<output>
After completion, create `.planning/phases/22-ai-budget-notifications-and-external-integrations/22-10-SUMMARY.md`
</output>
